# Agent Notes — warehouse

> **Durable memory across loop runs.** Read every run; append what you learn. Keep it tight.

## How to build / test this project

- Install:    `npm install` (should already be done; if `node_modules` is missing, run it)
- Typecheck:  `npm run check`   ← the ALWAYS gate — must be clean before marking any task done
- Build:      `npm run build`   ← run additionally for any task touching `client/`
- Logic tests: plain assertion scripts `script/test-warehouse-*.ts`, run via `npx tsx script/test-warehouse-<x>.ts` (exit 0 = pass). **No vitest/jest in this repo — do not add a test framework.**
- 🔴 **There is NO database in this environment** — no `.env`, no `DATABASE_URL`, no local Postgres. Never open a DB connection. `script/apply-warehouse.ts` and `script/seed-warehouse.ts` are WRITTEN here but only ever RUN by a human later. Logic tests must be DB-free: inject a fake `tx`/db object that records calls.

## The spec

- `SPEC.md` = the architecture plan (schema §4.1, movement engine §4.2, sync §4.3, UI §4.4, backlog §6). `SYNTHESIS.md` = the research it came from. The spec wins over your instincts; `BLOCKED.md` beats guessing.

## Conventions (house rules — violations have caused prod incidents)

- **Schema:** all new tables in `shared/schema.ts`, prefix `wh_`, copy the Drizzle patterns of the existing `shop_*` tables (same pk/timestamp/index helpers). Migration = ONE new file `migrations/2026-07-13_warehouse.sql`, additive only, `CREATE TABLE IF NOT EXISTS` / `CREATE [UNIQUE] INDEX IF NOT EXISTS`.
- **`script/apply-warehouse.ts`:** copy the newest existing `script/apply-*.ts` dry-run pattern (BEGIN → run migration → verify every table via `to_regclass` + every index via `pg_indexes`, assert partial indexes contain `WHERE` → ROLLBACK unless `--apply`). List EVERY new table and index in its verification arrays.
- **NO CHECK constraints on enum-ish text columns** (movement_type, reason_code, status…) — validate in `shared/warehouse.ts`. (A stale CHECK once 500'd the MFL checkout.) CHECKs only for true invariants (`delta <> 0`, non-negative money).
- **Derived, never stored:** `available` (= on_hand − active reservations), loan `overdue` (= due_on < today ∧ status='out'), PO `qty_received` (= Σ receipt movements). Do not add columns for these.
- **The ledger is append-only** — no UPDATE/DELETE code paths on `wh_movements`, ever. Stock corrections are new adjustment movements.
- **Atomic stock guard:** all cache updates go through `postMovementGroup` (SPEC §4.2) in one transaction with the ledger insert. Non-negative enforcement: a conditional `UPDATE … SET on_hand = on_hand + $delta WHERE on_hand + $delta >= 0` (insert row first if absent); 0 rows updated → reject the whole transaction with "insufficient stock at <LOCATION>". Guard skipped only when the item has `allow_negative`.
- **Money in CENTS** (integer). Quantities `numeric(12,3)` (Drizzle `numeric` maps to string in TS — convert explicitly at the edges).
- **Dates:** NZ timezone; use the repo's existing `nzTodayIso()` helper (grep for it) — never `new Date().toISOString()` for a calendar date, never round-trip an ISO date string through `Date`.
- **Routes:** new files `server/warehouse-routes.ts` (HTTP) + `server/warehouse.ts` (movement engine) + `server/warehouse-sync.ts` (channel sync); register routes in `server/index.ts` next to the shop routes registration. Auth: `requireAuth` + `requireTab("warehouse")` from `server/auth.ts` (org comes from the `X-Workspace-Slug` header). Requisition SUBMIT + own-requisition views are `requireAuth` only (any staff).
- **Tabs/UI wiring** (exact checklist): `shared/tabs.ts` → add `{ slug: "warehouse", title: "Warehouse", url: "/admin/warehouse" }` to `printsTabs` AND `"warehouse"` to `SUPER_ADMIN_ONLY_TABS`; `client/src/components/app-sidebar.tsx` → `printsNav` entry; `client/src/App.tsx` → routes; pages in `client/src/pages/warehouse-*.tsx` matching the visual style of the existing `prints-*.tsx` pages (dark premium, TanStack Query via `@/lib/queryClient`).
- **Frontend:** no CDN scripts — npm deps only, pinned exact versions. Scanner: `barcode-detector` ponyfill package (ZXing-C++ WASM, self-hosted assets) + native fast-path feature-detected via `getSupportedFormats()` contents (never `'BarcodeDetector' in window`). QR generation: the `qrcode` npm package.
- **Shopify:** GraphQL only (SPEC §4.3 / D9) via a minimal fetch-based client in `server/warehouse-sync.ts`; env vars per store (`WH_SHOPIFY_CUFC_TOKEN`, `WH_SHOPIFY_SIU_TOKEN`, store domains) read lazily; every push carries an idempotency key; mark mutation syntax `// TODO-verify(live)` — a human verifies against current shopify.dev before deploy. All sync behaviour behind env flags so the code is inert without config.
- **Do not touch** other features' code except the ONE flagged edit in T13 (smallest possible diff in `server/shop-routes.ts`). Never modify `deploy.sh`, `.github/`, existing migrations, or `drizzle.config.*`.
- **Commits:** one per task, message `loop(warehouse): T<n> <summary>`.

## Lessons learned (append-only)

- **2026-07-22 — THE TYPECHECK GATE IS `bash script/check-warehouse-gate.sh`, NOT a clean `npm run check`.** The canonical-2 base carries ~545 pre-existing tsc errors (routes.ts/schema.ts/storage.ts — snapshotted per-file in `script/BASELINE-TSC.txt`), so "check must be clean" was never achievable and is why the first loop runs stalled. The gate script passes iff warehouse-owned files have ZERO errors and no file regresses past its baseline count. Wherever PLAN.md/SPEC.md say `npm run check`, run the gate script instead. `npm run build` DOES pass at baseline and remains a hard gate for client-touching tasks.
- 2026-07-22 — a crashed earlier run mangled `package-lock.json` (restored from base 541b58a) and left a PARTIAL `shared/warehouse.ts` (2 tsc errors, no test script). T1 must finish/fix it, not start over blindly.
- 2026-07-22 (T1) — `const X: ReadonlySet<SomeUnionType> = new Set([...string literals])` does NOT typecheck: `new Set([...])` infers `Set<string>` from the literal array, which isn't assignable to `ReadonlySet<SomeUnionType>`. Always give the type param explicitly: `new Set<SomeUnionType>([...])`. Same trap will bite any future `Set`/`Map` built from a literal array and assigned to a narrower-typed field (e.g. future `wh_*` enum sets in the engine or sync code).
- 2026-07-22 (T2) — **`createInsertSchema(table).omit({...})` throws a spurious tsc TS2322 "Type 'boolean' is not assignable to type 'never'" on ANY table whose `id` column uses `.generatedAlwaysAsIdentity()`** (drizzle-zod 0.7.0 + zod 3.24.2 + TS 5.6.3 inference bug — reproduced in isolation, confirmed unrelated to which/how-many keys are passed to `.omit()`, confirmed NOT triggered when the table uses `serial("id").primaryKey()` instead). This is already the majority of shared/schema.ts's 154 pre-existing baseline errors (~154 of ~160 total `.omit()` call sites) — it is a pre-existing, file-wide issue, not something introduced by warehouse code, and NOT something to mass-fix (out of scope, huge diff, forbidden by "don't touch other features' code"). **Fix for any NEW `wh_*` insert schema: drop `.omit(...)` entirely and just export `createInsertSchema(table)`.** This is behaviourally identical whenever every field you'd have omitted (id, createdAt, updatedAt, processedAt…) already carries a DB default — drizzle-zod already infers defaulted columns as optional on insert without needing `.omit()`. Verify with a throwaway `const x: InsertX = { requiredFieldOnly: ... }` compiling fine. Same trap will bite every future `wh_*` insert schema (routes/engine tasks) that copies the `.omit({id:true,...})` convention from elsewhere in schema.ts.
- 2026-07-22 (T2) — Drizzle's column-level `.unique()` (e.g. `sku: text("sku").notNull().unique()`) does NOT correspond to any predictable index name in the hand-written SQL migrations this repo uses (no drizzle-kit push/generate here — schema.ts and the raw `.sql` migration files are two independently-maintained layers). Don't rely on Postgres's implicit `<table>_<column>_key` auto-naming for anything an `apply-*.ts` script needs to verify by name — give every unique constraint an EXPLICIT `CREATE UNIQUE INDEX IF NOT EXISTS <name> ON ...` in the migration (even a single-column one), and keep Drizzle's `.unique()` in schema.ts purely as TS-side documentation (harmless, no DB effect either way).
- 2026-07-22 (T3) — `server/db.ts` THROWS at module-EVALUATION time if `DATABASE_URL` is unset (`if (!process.env.DATABASE_URL) throw new Error(...)`), not lazily on first use. Any module that does a plain VALUE import (`import { db } from "./db"`) at its top level will crash the instant it's loaded — including by a `tsx script/test-*.ts` run, which has no `.env` in this worktree. `server/accounting/post.ts` (found in the sibling `budget-automation` worktree) already solved this: `import type { db as realDb } from "./db"` for TYPES ONLY (erased at compile time, never executes anything) + a lazy `const { db } = await import("./db")` *inside* the function body, only reached when that specific function actually runs. Copied this exact pattern into `server/warehouse.ts` (`runMovementGroup`, `reconcileStock`'s default). Any future warehouse module (routes, sync) that needs the real `db` must follow the same shape — a stray top-level `import { db } from "./db"` anywhere in a file that a test script imports (even transitively) will take the whole test run down with "DATABASE_URL must be set", and the failure looks nothing like the real bug.
- 2026-07-22 (T3) — A generic `Pick<typeof realDb, "insert" | "select" | ...>` fake (the accounting-precedent style) only stays easy to fake when every DB call is a SIMPLE `.values(row).returning()`/`.onConflictDoNothing().returning()` whose fake can dispatch purely on the VALUES it's handed. The moment a query needs a COMPUTED conflict action (`onConflictDoUpdate` with a `sql\`col + ${delta}\`` SET/setWhere) or a multi-table aggregate (a FULL OUTER JOIN + GROUP BY for reconcile), a plain object-literal fake can no longer introspect what the real drizzle call would do — the `sql` template's internal AST isn't meant to be pattern-matched by test code. Don't fight this: define a small BESPOKE interface (`WarehouseDb`/`ReconcileDb` — a few named async methods expressing the actual operations, e.g. `upsertStockLeg`, `listStockVsLedger`) that a plain in-memory-Map fake can implement directly, and keep exactly ONE real adapter function (`warehouseDbFromTx`, `reconcileDbFromRealDb`) that's type-checked against the schema but deliberately never unit-tested (it needs a live Postgres — that's a human/deploy-time concern, not a loop-agent one). This is the natural evolution of the accounting pattern once the query gets past "trivial single insert", not a departure from it — keep using the `Pick<realDb,...>` style for anything that stays trivial.
- 2026-07-22 (T4) — A plain HTTP CRUD/routes file (no engine-style transaction logic, e.g. `warehouse-routes.ts`) does NOT need the lazy-`db`-import trick from T3's lesson — that trick exists only because `server/warehouse.ts` is imported by a DB-free `tsx script/test-*.ts` run. A routes file is never imported by a test script (there's nothing to unit-test in "call db.select().where(...)"), only ever loaded by the real running server (which always has `DATABASE_URL`), and tsc doesn't execute code — so a plain top-level `import { db } from "./db"` is fine and is what every existing standalone routes file (`feedback-routes.ts`, `market-research-routes.ts`, `club-dossier-routes.ts`) already does. Don't over-apply T3's lesson here.
- 2026-07-22 (T4) — AGENTS.md's "register in `server/index.ts` next to the shop routes registration" is slightly imprecise: `registerShopRoutes(app)` actually lives inside `server/routes.ts` (called from within `registerRoutes()`), not `index.ts`. The real, already-established pattern for a NEW standalone feature module in this codebase is the `registerFeedbackRoutes`/`registerMarketResearchRoutes`/`registerClubDossierRoutes` trio — a dynamic `await import(...)` + call, inside the async IIFE in `index.ts`, after `registerRoutes()` and before the cron-job registrations. Used that for `registerWarehouseRoutes` too; future warehouse route-group tasks (T6–T9) that add whole new registration functions (vs. extending this same file) should follow the identical spot, not go hunting for a literal "shop routes" line in `index.ts`.
- 2026-07-22 (T4) — Master-data CRUD (items/locations/aliases) intentionally does NOT use `postMovementGroup`/`server/warehouse.ts` at all — creating/editing/deleting an item, location or alias never touches `wh_movements`/`wh_stock`. Keep it that way: only stock-affecting flows (T5 reservations onward) call into the movement engine. If a future task finds itself importing `runMovementGroup` from a CRUD handler in `warehouse-routes.ts`, that's a sign the operation actually belongs in a different route group.
- 2026-07-22 (T4) — A `zone` field that's DERIVABLE from another field the user already types (here: a location's `zone` from its own `code`, per the schema comment "first segment of a bin code, or the named zone itself") should be computed server-side on every write and never accepted from the client, even as an optional override — same "derived, never stored [as independently-editable]" spirit as `available`/`overdue`/`qty_received` elsewhere in this spec, just applied to a plain data-integrity field rather than a stock number. Added `deriveLocationZone()` to `shared/warehouse.ts` rather than inlining the one-liner in the route, so T16's admin UI can show the same derivation for a live preview without duplicating it.
- 2026-07-22 (T5) — Postgres does NOT allow `FOR UPDATE` on a query with `GROUP BY`/aggregate functions — so a "computed multi-row aggregate that also needs to serialize concurrent callers via row locks" (here: `available(item) = Σ on_hand(sellable bins) − Σ active reservations`) can't be one aggregate query with a lock clause bolted on. The real adapter (`reservationDbFromTx.getAvailableForItem`, `server/warehouse.ts`) has to be TWO plain `SELECT ... FOR UPDATE` statements (no GROUP BY, just the raw rows) with the summing done in JS after. This is a variant of T3's "bespoke ReservationDb/WarehouseDb seam, not `Pick<typeof realDb,...>`" lesson, but the specific new wrinkle (FOR UPDATE + aggregate can't coexist) is worth its own line for the next SQL aggregate this spec needs under a lock (T9's chargeback report and T11's count variance are both aggregates, though neither needs row-locking the way a concurrent reserve/reserve race does).
- 2026-07-22 (T5) — When one function's real DB work spans TWO bespoke seams at once (`consumeReservation` needs both `WarehouseDb`, to post the movement, and `ReservationDb`, to flip the reservation's status, inside the SAME transaction), don't inline `{ ...warehouseDbFromTx(tx), ...reservationDbFromTx(tx) }` at every call site — TS will usually infer the spread's type fine, but it's a needless re-check every time and reads worse than a single named `combinedDbFromTx(tx): WarehouseDb & ReservationDb` helper with an explicit return-type annotation (forces the assignability check to happen exactly once, at the helper's definition). The two source interfaces must have zero overlapping method names for the spread to be safe — true here (`WarehouseDb`'s 3 methods vs `ReservationDb`'s 6 don't collide) but worth checking again if a future task adds a 3rd seam to the mix.
- 2026-07-22 (T5) — A reservation that is item-level, not (item, location)-level (per `wh_reservations`' actual schema — confirmed by reading it rather than assuming from the SPEC prose), means `consumeReservation` posting its movement at a SPECIFIC location can still hit `InsufficientStockError` even though the item-level `available` check inside `reserveStock` already passed — reserving against total item availability across every sellable bin can't know in advance which single bin a later dispatch scan will actually name. This is not a bug to "fix" by pre-checking the target bin inside `reserveStock` (the bin isn't even known yet at reserve time, e.g. a webhook-driven reservation from an order) — it's the correct, expected two-layer guard (item-level at reserve, location-level at consume), and T5's own test suite had to deliberately construct a "enough overall, not enough in THIS bin" fixture to exercise it (a naive single-bin fixture can't reach this path, since a first-ever `(item,location)` row always succeeds regardless of sign — see T3's lesson on that). Any future task adding a second consume-shaped flow (e.g. T9 requisition pick) should expect the same two-layer possibility and not assume "it was reserved, so consuming it will always succeed."
- 2026-07-22 (T6) — When a PLAN.md line's prose is genuinely ambiguous ("over/short/damaged → QUARANTINE leg + discrepancy note" could mean ALL THREE route to quarantine, or only damaged does while over/short are just noted), resolve the ambiguity against the nearest hard constraint rather than the more elaborate reading: D15's reason-code enum has exactly one code that applies at receiving time (`damaged`) — no `over_receipt`/`pending_review` code exists, and inventing one is out of scope for a single task (it's a taxonomy change, "don't invent taxonomy" per D15's own rationale). So only damaged stock is quarantined; over/short are compared against an `expectedQty` (defaulted from the line's derived outstanding qty) purely to build a note string on the movement group. Documented this reasoning inline in both `shared/warehouse.ts` and the route handler so a future task doesn't "fix" it into routing over-receipts to quarantine without first adding a reason code for it.
- 2026-07-22 (T6) — A derived multi-row total split by a discriminant column (here: `qty_received` per PO line, split into good vs damaged by `reason_code`) is a single query with a Postgres aggregate `FILTER` clause (`SUM(delta) FILTER (WHERE reason_code = 'damaged')`), not two separate queries or a GROUP BY on the column itself — `reason_code` takes other values too (or is null), so grouping by it doesn't cleanly bucket into "good vs damaged". `sql.join(ids.map(id => sql\`${id}\`), sql\`, \`)` (already used in `server/routes.ts`) is the established way to splice a dynamic `IN (...)` list into a raw `sql\`...\`` template in this codebase — reach for that instead of drizzle's `inArray()` the moment the query is already a raw `db.execute(sql\`...\`)` (mixing a raw template with a builder-only helper like `inArray` doesn't work; `inArray` is fine standalone in a normal `.where()` chain, which the PO list endpoint's own aggregate-by-`whPoLines.poId` query uses instead, since THAT one has no FILTER and stays fully in the query builder).
- 2026-07-22 (T6) — CRUD "can this be deleted/edited" guards that depend on ledger history (e.g. "can't delete a PO/PO-line once anything's been received against it") can't be expressed as a DB FK constraint here, because `wh_movements.ref_id` is a deliberately un-FK'd polymorphic int (D15's `ref_kind`/`ref_id` pattern has to point at completely different tables depending on `ref_kind`, which Postgres can't express as one real foreign key) — so the guard has to be an explicit pre-delete/pre-update query against `wh_movements` in the route handler itself, not something the database will ever reject on its own. Any future task deleting/editing a requisition, loan, or count that already has movements referencing it needs the same manual guard, not an FK-driven 23503 catch.
- 2026-07-22 (T7) — **Always check for uncommitted work-in-progress from a crashed prior run BEFORE writing new code** — this run found `resolveScanCode`/`handleLocationMove`/the `/scan`+`/putaway`+`/transfer` routes AND a full `script/test-warehouse-scan.ts` already sitting in the working tree (untracked, from the "iteration 1" commit that updated `shared/warehouse.ts`/`server/warehouse.ts`/`server/warehouse-routes.ts` but apparently lost the moment before it could commit the test file or flip PLAN.md's checkbox). `git status`/`git log -- <file>` showed it as untracked, not "doesn't exist" — a plain `ls script/` first would have shown it too. Don't assume a task is unstarted just because PLAN.md still shows `- [ ]`; read the actual files the task would touch first. Since the code matched SPEC §4.4/D5/D7/D8 correctly and the tests passed clean, this run's job was verify-and-finish (run the gate + full test suite, add the PLAN.md Done note, commit) rather than redo. Future runs: `ls script/test-warehouse-*.ts` and `git status --short` are cheap first moves before assuming a blank slate.
- 2026-07-22 (T7) — Not every bespoke `*Db` seam exists for the same reason. T3/T5's `WarehouseDb`/`ReservationDb` are bespoke because the real queries (a guarded upsert, a row-locked aggregate) genuinely can't be expressed via a plain object-literal fake standing in for drizzle. T7's `ScanLookupDb` is bespoke for a DIFFERENT, much simpler reason — the three lookups themselves ARE trivial single-row `SELECT`s that a `Pick<typeof realDb,"select">`-style fake could probably shim — it's bespoke purely so `script/test-warehouse-scan.ts` can inject plain in-memory objects with zero drizzle machinery in scope at all. Don't assume every new `*Db` interface in this codebase implies "the query is uninterpretable by a plain fake"; say which reason applies in the file-header/function comment (as done here) so the next task doesn't hunt for a locking subtlety that isn't there.
- 2026-07-22 (T7) — A resolver that tries several lookups in priority order (here: LOC:-prefix → SKU → alias → bare location code → unknown) needs an explicit rule for what happens on a PARTIAL match, not just "keep trying the next one": a `LOC:`-prefixed code that doesn't match any location must return `unknown` immediately, never fall through to the SKU/alias tables just because they're checked later in the same function — the prefix is a positive claim ("this is a location"), and honouring a claim only when it succeeds while silently reinterpreting it on failure would make a mistyped bin label resolve as some unrelated item. Encode "this branch is terminal on a miss" as an early `return`, not as "falls through to the next `if` when the lookup returns undefined" — the two look identical for a MATCH but diverge for a miss, and only a test that deliberately makes the miss case ALSO satisfiable by a later branch (T7's fake seeds an item/alias for the exact same string used as the failing location code) will catch the difference.
- 2026-07-22 (T7) — `buildLocationMoveLegs`' return type is a plain structural interface (`LocationMoveLeg`, defined in `shared/warehouse.ts`) rather than importing `MovementLeg` from `server/warehouse.ts` — `shared/*` must never import from `server/*` (the dependency direction is fixed: server and client both import shared, shared imports neither). A tuple `[LocationMoveLeg, LocationMoveLeg]` is structurally assignable straight into a `MovementLeg[]` parameter with no cast needed as long as the field names/types line up (TS structural typing + array covariance) — so the two files can each keep their own name for "the same shape" without an adapter function in between. Worth remembering for any future shared helper that builds something destined for an engine-side `*Input` type.
- 2026-07-22 — This worktree has an automatic checkpoint commit firing after file edits (observed landing as `loop(warehouse): iteration 1` on top of the prior task's commit, containing exactly that task's diff). It's harmless — `git commit --amend` onto it with the proper `loop(warehouse): T<n> <summary>` message (after `git add` on any new untracked files) produces the correct single commit AGENTS.md asks for — but don't assume `git status --short` being empty at the START of a task means nothing changed; always `git log --oneline -3` first to see whether an auto-commit already captured a previous/crashed run's work that still needs finishing rather than redoing.
- 2026-07-22 (T8) — The auto-checkpoint mechanism from the T7 lesson above isn't limited to catching a crashed run's leftovers from BEFORE your session started — it fired mid-session here too (`loop(warehouse): iteration 3`, landing on top of T7's commit, containing a complete first-draft `GET /pick-queue` + `POST /dispatch` under the name `DISPATCH_SOURCE_KINDS`) even though `git log` showed a clean T7 HEAD at the very start of this run. Don't trust a single `git log`/`git status` snapshot taken once at the top of a task as ground truth for its whole duration — if a fresh `Read` of a file you're about to edit shows content you didn't write (a system reminder saying a file "was modified, either by the user or by a linter... this change was intentional, don't revert it" is the tell), stop, `git show <commit> -- <file>` to see the full diff, and verify-and-finish it (T7's move) rather than building a parallel, differently-named implementation of the same thing. Ending up with both `PICK_SOURCES`/`isPickSource` (mine) and `DISPATCH_SOURCE_KINDS`/`isDispatchSourceKind` (the checkpoint's) in shared/warehouse.ts would have been dead, confusing duplication — caught it before committing by re-reading the file the Edit tool refused to blind-write over.
- 2026-07-22 (T8) — When a request body/DB row needs BOTH an item's id and its `allowNegative` flag (D16) for a leg you're building BY HAND (not via `buildLocationMoveLegs` or `consumeReservation`, which already carry this concern), a `select({ id: whItems.id })`-only lookup is an easy, silent way to lose the negative-stock bypass for bulk materials — the guard then defaults to `false` (`postMovementGroup`'s `leg.allowNegative ?? false`) with no type error and no test failure unless a test specifically fixtures an `allow_negative` item hitting that exact code path. Whenever a new route hand-builds a `MovementLeg` (as T8's requisition-dispatch branch does, unlike T6/T7 which route through a shared leg-builder), grep the surrounding `db.select({...})` for whether `allowNegative` is actually in the projected columns, not just assume it's threaded through.
- 2026-07-22 (T8) — A status-transition endpoint that's gateable at the point stock actually moves (T6's `RECEIVABLE_PO_STATUSES` on receive) needs that SAME gate re-applied on every other endpoint that also moves stock against the same parent record — `GET /pick-queue` filtering to `status='approved'` requisitions is not, by itself, a guarantee that `POST /dispatch`'s requisition branch can't be called directly (with a `sourceId` from a `submitted`/`declined` requisition a client already has cached, or just guessed) unless dispatch ALSO loads and checks the parent's status itself. Filtering a list endpoint is a UX nicety, not a security/business-rule boundary — the mutating endpoint must always re-derive and re-check the real gate independently, never assume "it wouldn't be in front of the user if it weren't allowed."
- 2026-07-22 (T8) — When the same "is this a mailing address or a pickup" fact is needed in a NEW file, check whether the file that already owns that concept (`server/shop-routes.ts`, in 3 separate places) has a cheap convention (`!!order.addressLine1`) before reaching for what looks like the "more correct" normalized join (`shop_shipping_options.requires_address`) — the join can disagree with what was actually captured at checkout (a shipping option's own `requiresAddress` could change after the order was placed) and costs an extra query for no benefit the existing convention doesn't already give you. Grep the owning file for the fact you need before deriving it a new way.
- 2026-07-22 (T9) — Another checkpoint-commit handoff, but a partial one this time: `iteration 6` had built a genuinely good status-transition GRAPH (`REQUISITION_TRANSITIONS`/`isValidRequisitionTransition`/`allRequisitionLinesPicked`/`isValidMonth` in `shared/warehouse.ts`, plus the matching imports already added to `server/warehouse-routes.ts`) but zero route handlers — the T7/T8 lesson ("read what's there first, don't rebuild in parallel") applies even to a HALF-finished checkpoint, not just a fully-finished one: build ON the existing graph rather than inventing a second, differently-shaped one, even though nothing was calling it yet.
- 2026-07-22 (T9) — A status field that's "recomputed from evidence" (here: `picking`/`ready`, derived from `wh_requisition_lines.qty_picked` after every dispatch, mirroring T6's `derivePoStatusFromLines`) and a status field that's "an explicit human decision validated against a transition graph" (here: `approve`/`decline`/`collect`, via `isValidRequisitionTransition`) are TWO DIFFERENT KINDS of guard even when they share one enum and one transitions table — don't force the derived computation to also route through the discrete graph. Concretely: `REQUISITION_TRANSITIONS.approved` only lists `picking`/`declined`, never `ready` directly, because a HUMAN clicking straight from "approved" to "ready" with zero picking evidence would be a lie. But a single-line requisition fully picked in ONE dispatch call has real evidence for the full jump, so the derive function (`deriveRequisitionStatusFromLines`) computes `ready` directly off `allRequisitionLinesPicked` and does NOT consult `isValidRequisitionTransition` at all — that graph exists for the OTHER kind of guard. If a future task adds a manual override endpoint for `picking`/`ready`, gate THAT one through the graph; don't retrofit the auto-derive path to match it.
- 2026-07-22 (T9) — Introducing an intermediate status that a background/automatic process advances INTO (here: `approved`→`picking`, set inside `POST /dispatch`, not by the caller) means every OTHER endpoint that gates on the old status alone needs the SAME broadening, not just the write path — found two: `POST /dispatch`'s own requisition-status gate (`status !== 'approved'` would reject the very SECOND pick once the first had auto-advanced it to `picking`) and `GET /pick-queue`'s list query (`status = 'approved'` would drop a partially-picked requisition from the queue mid-pick, with qty still remaining). Grep every read of the enum column across the whole route file, not just the endpoint you're adding, whenever a task introduces a new reachable value for a status that other endpoints already gate on.
- 2026-07-22 (T9) — Before trusting a Done-note's prose description of what a PREVIOUS task's `ref_id` points at, re-read the actual committed code: T8's own Done note said the requisition dispatch branch posts `ref: {kind:'requisition', id: requisitionLineId}`, but the real code passes `id: sourceId` (the requisition's own id, not the line). This mattered directly for T9's chargeback-report join (`wh_movements.ref_id → wh_requisitions.id`, no line-table join needed) — trusting the prose would have produced a query joining to the wrong table/column. The reason it's not a bug: unlike a PO's `qty_received` (DERIVED from movements, so it NEEDS `ref_id` = line to disambiguate), `wh_requisition_lines.qty_picked` is a plain STORED column dispatch already writes directly — so `ref_id` never needed to resolve to the line at all. General lesson: a derived-vs-stored distinction on the target column is exactly the signal for whether a polymorphic `ref_id` needs line-level or parent-level granularity — check that before matching a join to a Done-note's words.
- 2026-07-22 (T10) — The auto-checkpoint mechanism (T7/T8/T9 lessons) can now flip a `PLAN.md` Tasks-list checkbox to `[x]` WITHOUT a matching Done-section note ever landing — found `- [x] T10 — see Done section` already checked at session start, but no T10 entry existed under `## Done` at all. Don't trust the checkbox alone as "this task has a recorded note, skip re-verifying" — always grep the Done section for the task number too, and if the code is real but the note is missing, write the note (this run's actual job) rather than assuming there's nothing left to do. Also: a `db.transaction()` opened directly in a ROUTE handler (not via `runMovementGroup`'s own lazy-tx wrapper) that calls `postMovementGroup(warehouseDbFromTx(tx), ...)` inline is the correct pattern whenever a NEW parent record (here: the `wh_loans`/`wh_loan_lines` insert) and its stock movement must commit/rollback as one unit — `runMovementGroup` alone can't span a caller's own additional inserts since it opens and closes its own transaction.
- 2026-07-22 (T10) — **A route that creates a brand-new parent record AND posts a movement in the SAME call cannot use `runMovementGroup`/`runConsumeReservation`'s convenience wrappers** — every task through T9 only ever posted movements against an ALREADY-EXISTING parent (a PO/requisition/order created in an earlier, separate call), so `runMovementGroup`'s self-contained transaction was always safe: on failure, nothing but the movement itself needed to roll back. Loan check-out (T10) is the first case where the parent (`wh_loans`/`wh_loan_lines`) is created in the SAME request as the movement — if `runMovementGroup` were used here, an `InsufficientStockError` would roll back only the movement's own transaction while the loan/lines insert (already committed moments earlier, outside that transaction) would be left behind as a dangling "checked out" record with no actual stock movement ever posted. Fix: open the route's OWN `db.transaction()`, do the parent inserts AND call `postMovementGroup(warehouseDbFromTx(tx), ...)` directly inside it (both succeed or both roll back), then manually call `notifyMovementCommitted(...)` afterward — which required exporting that function from `server/warehouse.ts` (it was module-private, only called internally by the `run*` wrappers). Any future task combining a new parent-record creation with a movement post (none currently planned, but worth checking if T11's count-approval or anything later does) should reach for this same pattern, not `runMovementGroup`.
- 2026-07-22 (T10) — **A client-supplied `idempotencyKey` is only safe on an endpoint whose OWN parent record is already idempotent (i.e. pre-existing, not created by this same call).** Every prior idempotencyKey-accepting endpoint (receive, putaway, transfer, dispatch, reservations) posts against a parent that already existed before the call, so replaying the same key is unambiguous: the movement's own dedupe is the whole story. Loan check-out is different — a retry with the same key would still insert a brand-new `wh_loans` row every time (nothing dedupes THAT), while `postMovementGroup` would find the OLD movement group by key and short-circuit with `alreadyProcessed: true` — producing a phantom second loan record whose returned `movement.groupId` actually belongs to a DIFFERENT (the first) loan's `ref.id`. Fix: check-out simply doesn't accept an `idempotencyKey` from the client at all (always passes `null`), accepting the same "a naive double-submit creates two records" reality that already exists for PO/requisition creation (neither of which take one either) rather than half-solving it in a way that's actively misleading. General lesson: before wiring a client idempotencyKey through to `postMovementGroup`, check whether the SAME call also creates the parent row the ref points at — if so, the key can't actually make the whole operation idempotent and shouldn't be offered as if it does.
- 2026-07-22 (T10) — When an array of "lines to process in this one call" references EXISTING rows by id (here: `loanLineId` in a return request) rather than describing brand-new rows, a duplicate id within the SAME array is a distinct bug class from duplicate ids in a create-style array (T9's requisition-submit duplicate itemIds are harmless — they just make two legitimate line rows). Loading the "already processed?" guard state ONCE up front (here: `existingById`, built before the loop) means a same-array duplicate sails past that guard on its second occurrence too (the map isn't mutated mid-loop), silently doubling the posted quantity. Explicitly reject duplicates in the raw input array (a plain `Set` size check) BEFORE using any pre-loaded guard state — don't rely on the guard alone.
- 2026-07-22 (T11) — A checkpoint mid-session can leave partial work sitting UNCOMMITTED in the working tree (not just in a prior commit, the T7-T9 form of this lesson) — found `COUNT_TRANSITIONS`/`isValidCountTransition`/`shouldHideExpectedQty` already added to `shared/warehouse.ts` as a plain working-tree diff (`git status` showed ` M shared/warehouse.ts` against a clean T10 HEAD) mid-way through this task's own `Read`s of the file — a second `Read` of the same offset showed DIFFERENT content than the first. The same "don't rebuild in parallel, read what's there first" discipline applies to an uncommitted diff exactly as much as a committed checkpoint: adopted `shouldHideExpectedQty` as-is rather than writing my own near-duplicate "should I hide expectedQty" check, and it ended up shaping the whole route design for the better (see next lesson). Lesson for future tasks: don't trust a single `Read` of a file as ground truth for the rest of a task if there's any gap before you act on it — a repeat `Read`/`git status --short` immediately before editing is cheap insurance.
- 2026-07-22 (T11) — `shouldHideExpectedQty(blind, status)` being a function of BOTH the session's blind flag and its CURRENT status (not just blind) implies a design that's easy to miss if you only skim the name: it means there is exactly ONE session/lines view whose visibility adapts per-request, not two hardcoded "counter view" vs "operator view" endpoints. Route design followed from that: `GET /counts/:id` calls this on every response (hidden through `open` AND `submitted`, shown once `approved` — blind counting is about the whole in-progress lifecycle, not just the data-entry moment), while the approver's pre-approval "variance list" (PLAN's own named deliverable) needed a genuinely SEPARATE endpoint (`GET /counts/:id/variance`) that is always-full regardless of blind/status — because the approver has to review variances to decide whether to approve, which is fundamentally incompatible with hiding them until after that decision is made. Don't try to satisfy "variance list" by adding a query param to the blind endpoint instead of a separate route — a toggle on the same URL is one accidental keystroke away from leaking the blind data to whatever UI was built for counters.
- 2026-07-22 (T11) — D12 literally says "posts **an adjustment movement**" for approved count variance — `MovementType` has BOTH `adjustment` and `count` in its taxonomy (D15), and it's tempting to reach for `count` since that's the feature name, but the spec's own wording picks `adjustment` specifically; `count` is left free for a possible future pure "this count event happened" ledger entry that isn't a stock correction. When a decision doc uses a specific word from a fixed enum, match that word literally rather than the feature's own name — the same "resolve ambiguity against the nearest hard constraint" move as T6's damaged-only-quarantine call.
- 2026-07-22 (T11) — Approve is the SECOND case (after T10's loan check-out) where a status-transition endpoint's write can't safely use `runMovementGroup`'s self-contained transaction, but for a DIFFERENT reason than T10's: here the parent (`wh_counts`) already exists, so it's not "movement + new-parent-insert must roll back together" — it's "movement-post + status-flip must be ATOMIC so a retry after a partial failure can't re-post the same adjustment a second time." Approve takes no client `idempotencyKey` (mirroring requisition approve/decline/collect's status-guard-only dedup), so `canApproveCount`'s status check is the ONLY thing stopping a double-post — if the movement committed via `runMovementGroup` but a separate later `db.update(whCounts, {status:'approved'})` call failed or was never reached, a retry would still see `status:'submitted'`, pass the guard, and post the SAME adjustment legs again. Fix: `postMovementGroup(warehouseDbFromTx(tx), ...)` and the `whCounts` status update both happen inside ONE `db.transaction()`, exactly like T10's checkout, with `notifyMovementCommitted` called manually afterward. General lesson: whenever a status-transition guard is the sole idempotency mechanism (no client key), the guard's own state (the status column) MUST be updated in the same transaction as the movement it's guarding — not just when a new parent row is also being created.
- 2026-07-22 (T13) — Importing a top-level function (`runReserveStock`) from `server/warehouse.ts` into an ordinary, always-has-a-real-`DATABASE_URL` route file (`shop-routes.ts`) is completely safe and needs none of T3's lazy-import ceremony — that ceremony exists only to protect DB-free `tsx script/test-warehouse-*.ts` runs, which never import `shop-routes.ts` (or anything that imports it) at all. The rule is about the IMPORTER's exposure to test scripts, not about which module is being imported.
- 2026-07-22 (T13) — When a PLAN.md task line says "the payment-confirm path" (singular) for a feature that actually has TWO independent finalize functions sharing the same idempotency doctrine (`finalizeShopOrderPaid` for a normal order, `finalizeTeamOrderAllPaid` for a Player-Pay/team-kit order whose stock decrement resolves colour+roster-size rather than a plain `item.variantId`), resolve it to the simpler/more literal one rather than generalizing to "every place stock moves on payment" — matches both PLAN's own wording and "smallest possible diff", and the more complex path would need a whole new colour+size→variant→wh_item resolution chain that's out of scope for a dark-launched integration task. Noted this explicitly in the Done note so a future task extending team-order reservations doesn't mistake the omission for an oversight.
- 2026-07-22 (T14) — The "don't trust one snapshot of a file's contents for a whole task's duration" lesson (T8/T11/T12) now has a variant with no `git log`/`git status` evidence at all: this task's SECOND `Read` of `client/src/pages/warehouse-labels.tsx` (already-found, uncommitted from a crashed prior run) came back missing a CSS rule (`@media print { .wh-print-hide { display: none !important; } }`) that the FIRST `Read`, minutes earlier in the same task, clearly showed — while `git status --short`/`git log --oneline` stayed unchanged (still just the same untracked file + the unrelated `package.json`/`package-lock.json` diff) the entire time, and the Edit tool's own "file had been modified on disk since you last read it" notice was the only signal. An untracked file's on-disk bytes can drift mid-task with zero git trace (no commit, no stash) — for an UNTRACKED file specifically, `git status`/`git log` prove nothing about content stability, only a fresh `Read` immediately before an `Edit` does. Don't assume a missing git diff means nothing changed.
- 2026-07-22 (T14) — When a print stylesheet needs an element to be BOTH "the thing print CSS targets and unhides via `position:fixed` + `visibility:visible`" AND "visually part of an on-screen preview box the surrounding chrome-hiding container would otherwise swallow", the fix is nesting depth, not `display`/`visibility` choice: an ancestor's `display:none` unconditionally hides ALL descendants (a descendant CANNOT override it), while an ancestor's `visibility:hidden` CAN be overridden by a descendant's own `visibility:visible`. So the real print source must live OUTSIDE any `display:none`-driven "hide the chrome" container, but CAN safely live inside a plain bordered preview `<div>` with no special class at all — that div receives `visibility:hidden` from the blanket `body * { visibility:hidden }` rule same as everything else, but its `.wh-print-area` child overrides back to visible, and the child's own `position:fixed` breaks it out of the (invisible, non-`display:none`) parent's flow onto the printed page regardless of DOM nesting. Don't reach for "render the print source twice" (once hidden for screen preview, once for print) to solve this — same content, one render location, correct CSS semantics is enough.
- **2026-07-22 (T16a) — Route-path convention for T17 (binding, not a suggestion):** the 3 new admin pages (`warehouse-dashboard.tsx`/`warehouse-items.tsx`/`warehouse-locations.tsx`) and the 2 pre-existing ones (`warehouse-scan.tsx`/`warehouse-labels.tsx`) already link to each other using nested paths: dashboard at the tab's own `/admin/warehouse`, everything else at `/admin/warehouse/{items,locations,pos,requisitions,loans,counts,sync,ledger,scan,labels}`. T17 must wire `App.tsx`'s routes to these EXACT paths, not invent different top-level ones (the `print-*` pages' flat `/admin/print-jobs`-style convention was considered and rejected here specifically because one warehouse tab has ~10 sub-sections vs prints' handful, and nesting keeps them visually grouped in the sidebar). Verified these 3 pages actually bundle under Vite (not just tsc) using T14/T15's exact move: temporarily wire imports+routes into `App.tsx`, `npm run build` (module count 3730→3734), then `git checkout -- client/src/App.tsx` to revert.
- 2026-07-22 (T16a) — Before assuming a dashboard/aggregate page can be built from existing endpoints, grep for whether ANY route already returns the aggregate shape you need (`grep -n "app\.\(get\|post\)" server/warehouse-routes.ts` lists every route in one shot) — T4-T15 built rich CRUD + lifecycle endpoints but never a cross-item aggregate (no route ever summed `wh_stock` by item, or listed the ledger unfiltered/per-item). When PLAN's own bullet names specific data shapes ("low stock vs min_qty", "recent movements feed") that no endpoint yet computes, adding the read endpoint IS in scope for a client-page task as long as it lives in the feature's own route file (`server/warehouse-routes.ts` here) — it isn't scope creep, it's the task's actual dependency. Don't reach for 4-5 separate small endpoints when one aggregated `GET /dashboard` avoids extra round-trips and keeps the "what does the dashboard need" question answerable by reading one handler.
- 2026-07-22 (T16a) — A nested `function` DECLARATION (not a `const foo = () => {}`) defined ANYWHERE inside `registerWarehouseRoutes(app: Express) { ... }` is callable from code placed EARLIER in the same function body, because function declarations hoist through the whole enclosing function scope — confirmed by placing the new `GET /dashboard` handler near the very top of the file (before the `Locations` section) while it calls `fullName()` (defined ~1100 lines later, near the requisitions section) and `withOverdue()` (defined ~1700 lines later, near the loans section) with zero tsc complaint and a clean gate run. Don't feel obligated to physically relocate a new route to sit after every helper it uses, or duplicate a helper just because it's "declared below" — check whether it's a `function` statement (hoists) vs a `const`/arrow (doesn't, TDZ) before assuming you need to move anything.
- 2026-07-22 (T12) — **The auto-checkpoint collision went one level further than T7-T11's "read an existing draft first": a SECOND, genuinely concurrent agent run shared this exact working tree at the same time as this one, and the two runs' file writes landed INTERLEAVED in a single auto-commit** (`loop(warehouse): iteration 2`), not cleanly before-or-after each other. Evidence: `git log` at session start showed a clean T11 HEAD with an empty `git status`; partway through the session (discovered only by re-checking `git log` right before the final commit — the T8 lesson's own advice, worth repeating) HEAD had moved to a new commit containing (a) `shared/schema.ts`/`migrations/2026-07-13_warehouse.sql`/`script/apply-warehouse.ts`/`shared/warehouse.ts`/`script/test-warehouse-shared.ts` edits BYTE-FOR-BYTE IDENTICAL to this run's own (confirmed with `git diff <T11-sha> <checkpoint-sha> -- <file>` — not just similar, literally character-identical, including comments), (b) a `BLOCKED.md` hand-off note this run never wrote, and (c) an unused `findReservationForRef` export added to `server/warehouse.ts` this run never wrote either. Two DIFFERENT agent processes, working the identical task from the identical PLAN/SPEC/AGENTS context, independently reasoned their way to the identical schema fix (the `wh_movements`/`wh_reservations.ref_id` int→bigint widening, `wh_shopify_variant_links`) — believable since that fix has essentially one correct shape — but each also wrote its OWN version of `server/warehouse-sync.ts`/`server/warehouse.ts`-additions that the OTHER couldn't see. This run's own `Write` call for `server/warehouse-sync.ts` silently overwrote whatever the other run's checkpoint had put there (Write replaces the file at that path unconditionally) — by the time this was discovered, the other run's draft no longer existed on disk to reconcile against, only in `git show <sha>:server/warehouse-sync.ts` history. **Resolution: kept the identical schema work (zero risk — it matches character-for-character), kept this run's own fully-tested `server/warehouse-sync.ts` (the other run's own hand-off note admitted 2 unresolved tsc errors and no test file yet, so there was no version-quality argument for reverting to it even if that were still possible), deleted the now-stale `BLOCKED.md`, and removed the orphaned `findReservationForRef` (this run's equivalent partial-refund-release logic composes the ALREADY-EXPORTED `reservationDbFromTx`+`releaseReservation` instead, needing no new export) to leave exactly one way to do everything.** General lesson for any future task in ANY loop worktree: a clean `git status`/`git log` at task START is not proof of exclusive access for the task's DURATION (T8's lesson already said this for a single run's own mid-session checkpoints) — when the final pre-commit `git log` shows content you didn't write, don't just assume it's your own earlier work auto-committed; diff it file-by-file against what you actually intended, because some of it may be a genuinely different concurrent process's contribution that needs its own foreign artifacts (stray hand-off docs, orphaned exports, alternate never-wired implementations) cleaned out, not silently kept or blindly discarded.
- 2026-07-22 (T15) — **The T12 concurrent-collision pattern is not a one-off — it recurred, in a MILDER but still notable form, on the very next task.** This run began normally (read AGENTS.md/PLAN.md/SPEC.md, confirmed a clean `loop/warehouse` HEAD with empty `git status`, planned a from-scratch `client/src/pages/warehouse-scan.tsx` + a new `server/warehouse-routes.ts` `POST /consume` endpoint + a self-hosted `client/public/zxing/zxing_reader.wasm`) — but a `git status` check taken purely as due diligence RIGHT BEFORE the first `Write` call (no prior signal anything was wrong) showed `server/warehouse-routes.ts` already modified and an untracked `client/public/zxing/` present, neither of which this run had touched yet. A concurrent process was building the identical T15 independently, in real time, in the same working tree — and by the time the first `Write` for `warehouse-scan.tsx` was attempted, that process had ALREADY finished the entire page (1430 lines, fully working, its own `useCamera` hook, action forms for every T4-T13 endpoint) and the tool's own "read the file first" guard surfaced it before any content could be clobbered. Minutes later, mid-verification, the whole thing (schema-adjacent `printOrders` import + the new `/consume` route + the wasm asset + the page + a complete PLAN.md Done-section note) landed as a single already-committed `loop(warehouse): T15 ...` commit, indistinguishable from a normal prior run's output. **The response was the same as T7-T12's, just faster to trigger: stop, verify (independently re-derive every server endpoint's request/response shape from `warehouse-routes.ts` and cross-check the found `warehouse-scan.tsx` against it line-by-line rather than trusting it), run the full gate + build + every `test-warehouse-*.ts` suite fresh, confirm `git status` is clean, and only THEN treat the task as done — don't write a competing version just because your own plan was already fully formed.** Compounding lesson: this run had independently arrived at the exact same otherwise-missing gap the concurrent run found — no T4-T11 route ever actually posted the `consume` MovementType (only present in the taxonomy arrays) — and would have built nearly the identical single-outbound-leg `POST /consume` (`ref: {kind:'print_order', id}`, validated against the existing `print_orders` table) had the other run not gotten there first; when two independent readings of the same SPEC/AGENTS/PLAN converge on the same fix, that's a strong signal the fix is right, not a coincidence to be suspicious of. **Practical takeaway for any future task in this worktree: treat "check `git status` immediately before every `Write`/`Edit`, not just at task start" as mandatory, not paranoid — on this loop setup it has now caught a live, in-progress collision, not just a stale one.**
- 2026-07-22 (T16a) — **The T12/T15 collision pattern recurred a third time, and this time it fully resolved (built, tested, committed) entirely inside the polling window of a SINGLE verify-and-finish run**, with no code of this run's own ever written first: `git status` was clean at session start, but a `git diff server/warehouse-routes.ts` taken as due diligence right after reading SPEC/PLAN already showed a partial (77-line, then 234-line two checks later) in-progress diff; polling `git status`/`git diff` on a plain sleep loop (~10-15s intervals) showed the 3 target `.tsx` files appear one at a time over roughly 4 minutes, then `PLAN.md` and a momentary `App.tsx` diff (the T14/T15 temporarily-wire-then-revert bundling check), then a clean tree with a new commit. **Don't rush to write your own version the instant you see a partial diff — if the working tree is still changing, it's cheaper to poll (a plain `for`/`sleep` loop checking `git status --short`/`git diff --stat` until two consecutive checks match) than to race a concurrent process to the same file** (a real `Write` mid-collision is what silently destroyed the other run's draft in T12). Once stable, treat it exactly like T7-T15's "verify, don't rebuild": re-derive correctness independently (every new SQL column name checked against `shared/schema.ts` directly, not trusted from the diff's own comments) rather than assuming a committed message means it's already been reviewed by anyone.
- 2026-07-22 (T16a) — **A verify pass must still hunt for real bugs even when the gate/build/tests all pass clean** — tsc and the DB-free logic-test suites can't catch a React state-lifecycle bug, because nothing about "component compiles and unrelated shared-logic assertions still pass" exercises "what happens when this same modal instance is reused for a different record." Found two: (1) `warehouse-items.tsx`'s `<ItemDetailModal>` had no `key={detailId}` — since the component is never unmounted (React keeps the same instance across re-renders, only the internal `if (itemId === null) return null` branch toggles visibility), its `editing`/`form` local state survived closing the modal and reopening a DIFFERENT item, so a user mid-edit on item A who closed without saving and then opened item B could see B's detail already sitting in "edit mode" pre-filled with A's stale field values — hitting Save there would silently write A's data onto B's row. (2) `warehouse-locations.tsx`'s "New location" modal instance is permanently mounted with `location={null}` (no key, since there's only ever one "new" record, unlike the per-id-keyed edit instance below it) and never reset its 3 fields after a successful create, so a second "New location" click pre-filled the just-created values instead of a blank form. **The general check worth doing on any modal/form component reused across DIFFERENT records via a changing id prop (not a boolean open/close toggle): does it have a `key` tied to that id, OR does its success handler explicitly reset every field it owns?** Grep for `<SomeModal` call sites lacking a `key=` prop whenever the same component renders for more than one distinct record over its lifetime (a single always-`null`/always-"new" instance still needs the reset-on-success half even without a key, since there's nothing to key BY). Fixed both with the minimum-diff option already established elsewhere in this same PLAN: a `key` for the multi-record case (matching the sibling Locations page's own already-keyed edit instance and `prints-materials.tsx`), a same-file `onSuccess` reset for the single-instance create case (matching `warehouse-items.tsx`'s own `CreateItemModal`).
- 2026-07-22 (T16b) — Admin pages 2 (`warehouse-pos.tsx`/`warehouse-requisitions.tsx`/`warehouse-loans.tsx`) needed ZERO new backend endpoints — every action (PO create/patch/lines/receive, requisition submit/approve/decline/collect, pick via existing `POST /dispatch` with `sourceKind:'requisition'`, loan check-out/return) already existed from T4-T10. Applied T16a's own verify-pass finding pre-emptively rather than waiting to discover it again: every detail modal keyed by a changing record-id prop needs `key={id ?? "none"}` at its render site, or its local form state (edit-mode fields, a half-typed decline reason) survives closing one record and reopening a different one. Check this on every future modal that's conditionally populated by a prop id rather than unmounted/remounted.
- 2026-07-22 (T16c) — Admin pages 3 needed 3 new read/action endpoints (`GET /movements`, `GET /sync-state`, `POST /sync-state/:itemId/push`, `POST /reconcile-poll`) that T12 had already anticipated in its own comments: `wh_sync_state.pending` is written `false` unconditionally (see its schema.ts comment) — the LIVE "currently queued" signal for a sync dashboard is `getPendingPushItemIds()` (T12's exported debounce-queue reader), never the DB column. Read a prior task's own forward-looking comments (grep the DB column's schema.ts comment, not just the current task's own code) before assuming a stored flag is the source of truth. Also confirmed (again) that importing plain functions from `./warehouse-sync` at the top of `warehouse-routes.ts` needs none of T3/T12's lazy-`db`-import ceremony — that trick only protects modules a DB-free `tsx script/test-warehouse-*.ts` run actually imports, and no test script imports `warehouse-routes.ts` (grep first, per T4's identical lesson one file over).
- 2026-07-22 (T17) — The concurrent-collision pattern (T12/T15/T16a/T16b) showed a new variant: a `git status --short` at session start showed `client/src/App.tsx` already modified (a partial, 3-page wiring diff — leftover from T16c's own temporarily-wire-then-revert bundle check, apparently never reverted). A SECOND check taken moments later, before writing anything, showed that same file back to a clean, unmodified state — no commit landed in between (`git log` unchanged throughout). Whether this was a genuinely concurrent process finishing its own revert or a filesystem/read timing artifact couldn't be determined and didn't need to be — the response is the same either way: re-`git status`/re-`Read` immediately before every `Write`/`Edit` (already-established discipline), and don't build your own change on top of a snapshot that's more than a few seconds old. Separately, mid-task a genuinely different, unrelated file (`server/warehouse-routes.ts`) picked up its own uncommitted diff (a timezone bugfix to T16c's date-range filter) that had nothing to do with T17's own wiring work and stayed stable across two checks — evidence some other concurrent process really was active this run, just not colliding on the same files. Left it alone entirely (didn't stage, didn't commit, didn't revert) since it wasn't this task's scope and touching someone else's in-flight, untested diff is worse than leaving it for a future run to verify-and-finish (the established move for every prior collision).
- 2026-07-22 (T17) — Wiring a single super-admin-only feature tab with MANY sub-pages (warehouse: 1 dashboard + 10 sub-pages) needs exactly ONE `shared/tabs.ts` entry and ONE `app-sidebar.tsx` `printsNav` entry — not one per sub-page. `canAccessTab`'s whitelist check gates the whole `warehouse` slug; every sub-page is reached by an in-page link (`setLocation()`) from the dashboard or a sibling page, never from the sidebar directly. This mirrors how the existing `jobs`/`orders`/etc. single-entry pattern already works for other multi-page prints features — don't add N tab entries for what's really one feature with internal navigation. The real per-page security boundary is server-side (`requireTab("warehouse")` on every API route, already built T4-T16c) — the sidebar/tab wiring is pure UX, not a second enforcement layer.
- 2026-07-22 (T17) — Before wiring routes for a batch of already-built-but-unwired pages, grep every one of them for the literal path strings they already use to link to each other (`grep -rn '"/admin/warehouse' client/src/pages/warehouse-*.tsx`) rather than re-deriving the convention from a Done-note's prose — T16a's own note stated the binding convention, but the pages' actual `setLocation()`/query-key calls are the ground truth for exactly which paths must resolve (a prose description can drift from the code that was actually built across T14-T16c's several separate sessions). All 11 paths matched the stated convention exactly here, but treat that as something to confirm, not assume.
- 2026-07-22 (T16b) — **Fourth occurrence of the T12/T15/T16a live-collision pattern, and this run never wrote a line of its own code**: `git status` was clean at session start (T16a HEAD), but polling `git status --short` every ~15s showed `warehouse-pos.tsx` → `warehouse-requisitions.tsx` → `warehouse-loans.tsx` appear one at a time over roughly 13 minutes, then a momentary `App.tsx` diff (the bundle-check) and a `PLAN.md` diff, then a clean tree with a new commit (`92ba435`) already carrying its own full Done note and an AGENTS.md lesson. This run's entire job was independent re-verification (cross-check every request/response shape the 3 pages assume against the actual route handlers in `server/warehouse-routes.ts`, and every schema field they reference against `shared/schema.ts` — not just re-read the Done note's own prose) plus the full gate/build/regression suite fresh; found zero code defects. **Found one real defect anyway, in the PLAN.md prose itself, not the code:** the committing run's `Edit`-based insertion of the new `- [x] T16b — ...` bullet landed it *between* T16a's own bullet and T16a's pre-existing indented "Verify-and-finish pass" sub-bullet (a leftover from T16a's own verify-and-finish run) — because the anchor text used for that insertion was T16a's bullet alone, which doesn't include what already followed it. The sub-bullet is entirely about `warehouse-items.tsx`/`warehouse-locations.tsx` (T16a's pages) but now visually reads as if it's T16b's own verify note. Reordered so each entry's sub-bullet stays adjacent to the entry it actually describes. **General lesson: when inserting a new `- [x] T<n> — ...` line into an already-populated Done section via a string-replace, check what already follows your chosen anchor point, not just what precedes it** — an indented sub-bullet belonging to the entry immediately above your insertion point will silently get pushed below your new content otherwise, and nothing (gate/build/tests) will ever catch a misattributed prose note.
