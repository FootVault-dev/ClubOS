# AGENTS.md — AttributionOS build contract (loop/attribution)

Background reading (once, if unsure about a design decision):
`/Users/danielmeyn/Desktop/AIOS/DanielMeynOS/outputs/deep-research/2026-07-03-attribution-gold-standard/synthesis.md`
Master plan: `/Users/danielmeyn/Desktop/AIOS/DanielMeynOS/plans/2026-07-03-attribution-os-build.md`

## What we're building (one paragraph)
First-party attribution for 6 sports brands inside this app: a visitor/touchpoint event
spine with server-set cookies, a persons+identities layer (PostHog merge rules), a
short-link/QR layer (Dub mechanics), UTM-instrumented email, Meta ad-level joins
(url_tags → ad.id → spend sync), and an admin Attribution dashboard with journey
timelines. Conversions in Postgres are the revenue truth; models are computed at query
time (first-touch, last-non-direct, lifetime-first only).

## HARD RULES (violating any of these ruins the run)
1. **NEVER start the server** (`npm run dev`, `tsx server/index.ts`, etc.) — boot against
   the prod DATABASE_URL triggers live crons (league balance charging). Never read or use
   `.env` `DATABASE_URL`. All verification is build + pure-logic tests.
2. **NEVER apply migrations or run `drizzle-kit push`/`db:push`** — prod has schema drift;
   push drops columns. You WRITE migration `.sql` files (additive only: CREATE TABLE IF NOT
   EXISTS / ALTER TABLE ... ADD COLUMN IF NOT EXISTS / CREATE INDEX IF NOT EXISTS) and
   mirror them in `shared/schema.ts`. A human applies them.
3. **NEVER deploy** (`deploy.sh`, `fly`, `vercel`), never `git push`, never call live
   Stripe/Meta/Resend APIs. No network calls in tests.
4. **No child PII in any event or CAPI payload, ever.** The attribution person is the
   PARENT/payer. Do not add child names/DOBs to analytics_events, links, or Meta events.
5. **Additive, non-breaking:** existing funnels must keep working unchanged. New code
   paths are opt-in and defensive (attribution failures must never block a checkout —
   wrap in try/catch, log, continue).

## Design decisions you implement (do not re-litigate)
- **Cookies:** visitor id `usg_vid` (2 years) and click id `usg_cid` (90 days) are set via
  HTTP `Set-Cookie` from Express (SameSite=Lax, Secure, NOT httpOnly — client script reads
  them), never from JS. Server middleware owns them.
- **Canonical channels** (utm_source normalised): facebook, instagram, google, email,
  whatsapp, sms, qr, referral, organic, direct, other. Synonym map at ingest: fb→facebook,
  ig→instagram, msg→messenger, an→audience_network, google-ads→google, etc. ALWAYS keep the
  raw value in a `_raw` column alongside.
- **Classifier precedence** (GA4-style, pinned): referral-code param → our click id (?ci=) →
  paid (utm_medium in {paid,cpc,ppc,paid_social} OR meta ad params present) → utm channel →
  known-referrer table (l.facebook.com, lm.facebook.com, l.instagram.com,
  android-app://com.instagram.android, t.co, google organic domains) → direct.
  **fbclid with NO utm/ad params = channel `meta_unattributed`, NEVER paid** (fbclid appears
  on organic clicks too).
- **Meta macros are open sets:** store raw placement/site_source_name; map known values;
  bucket unknowns as `other_<raw>`; any value containing `{{` is nulled (unreplaced-macro bug).
- **Conflict waterfall** on conversions: referral code → click-id/UTM tracked → self-reported
  (HDYHAU) → unattributed. Deterministic. Never blend.
- **Identity merge rules (PostHog verbatim):** anonymous→identified merge freely; NEVER
  auto-merge two already-identified persons; blocklist of illegal ids ('undefined','null',
  'None','[object Object]','NaN','anonymous','guest',''); every merge writes a
  `person_merges` audit row; retroactive stitch = UPDATE analytics_events SET person_id
  WHERE visitor_id = X AND person_id IS NULL.
- **Models at query time** over the touchpoint log: first_touch, last_non_direct,
  lifetime_first. Lookback window is a query param (default 90d). New-vs-returning is a
  first-class split. No fractional/U-shaped/time-decay models.
- **Purchase event dedup:** ONE deterministic event id `purchase_<registrationId>` used by
  BOTH the browser pixel call and server CAPI for the same purchase.

## Codebase conventions (follow the neighbours)
- Server: endpoints in `server/routes.ts` (public: `/api/public/...` with explicit CORS
  where cross-origin; admin: `requireAuth` + tab gating via `requireTab(<slug>)` where used).
  Storage methods on the class in `server/storage.ts` (interface + impl). Crons follow
  `server/league-balance-cron.ts` / `server/print-cron.ts` patterns (registered in
  `server/index.ts`, guarded, NZ timezone-aware).
- Schema: `shared/schema.ts` drizzle + `createInsertSchema(...)` insert schema + type
  exports, matching the existing style. Money in cents. Timestamps `timestamp(...)`.
- Client admin: pages in `client/src/pages/`, tab slugs registered in `shared/tabs.ts` and
  the sidebar (`client/src/components/app-sidebar.tsx`), react-query with exact query keys
  (invalidate with the same key shape you queried), dark premium UI (`premium-input`,
  `rounded-2xl border border-blue-500/10 bg-white/[0.02]` panels), `data-testid` attributes.
- Public funnel pages: per-brand BRAND consts pattern (see `mfl-*.tsx`), wouter routing in
  `client/src/App.tsx` (3-segment routes BEFORE 2-segment `/league/:slug`).
- Shared pure logic in `shared/` (see `shared/league-pricing.ts` + its test
  `script/test-league-pricing.ts` — plain tsx + assert, run with
  `npx tsx script/test-league-pricing.ts`). Copy that testing pattern.
- Dates: NZ local — never `toISOString().slice(0,10)` for NZ dates.

## Verification (what "done" means)
- `npm run build` exits 0 (vite + esbuild). NOTE: `npm run check` (tsc) has ~404
  PRE-EXISTING errors — do NOT try to fix them, do NOT use tsc as your gate; your NEW code
  should still be written type-correctly.
- Pure-logic tasks ship a `script/test-attribution*.ts` runnable with `npx tsx`, exiting
  non-zero on failure, and you RUN it.
- UI tasks: build green + the page/tab wired into routing and sidebar registries.

## Lessons (append below as you learn)
- macOS bash is 3.2 — avoid mapfile/associative arrays in any shell you write.
- `apps/clubos` is its own git repo (nested). Commit here, not the parent.
- Run a pure-logic test with `npx tsx script/test-<name>.ts` (exits non-zero on fail). `npm run build` = vite + esbuild, ~7s, exits 0 clean (chunk-size + `dist/index.cjs 2.1mb ⚠️` warnings are PRE-EXISTING noise, not failures).
- **T1 shipped** `shared/attribution.ts` — the shared vocab/classifier all later tasks build on. Key exported surface to reuse (don't reimplement): `classifyTouch(TouchInput): ClassifiedTouch` (channel/channelRaw/isPaid/tracked/matchedBy), `resolveConversionAttribution(ConversionSignals)`, `normalizeSource`, `classifyReferrer`, `cleanMacroValue`, `isValidExternalId`, `validExternalId` (cleanMacro+isValid combined, returns cleaned-or-null — use this before keying anything on an external id), `mapHdyhauToChannel`, consts `CANONICAL_CHANNELS`/`ALL_CHANNELS`/`SYNONYM_MAP`/`PAID_MEDIUMS`/`ILLEGAL_IDS`/`META_UNATTRIBUTED`.
- Decisions I made where T1's spec was under-specified (kept consistent with GA4, safe to rely on downstream): **gclid ⇒ paid google** (gclid only appears on Google Ads clicks, unlike fbclid); **?ci= sets a `tracked` flag, not a channel** (our redirect always appends the link's utm, so channel comes from those); **unknown external referrers → direct** (only the KNOWN-referrer table classifies, per pinned precedence); **unknown non-empty utm_source → `other`** with the raw value preserved in `channelRaw`.
- `classifyTouch` hygiene-checks every id via `validExternalId` BEFORE it counts, so a literal `{{ad.id}}` macro never registers as a paid ad param. Reuse that pattern at ingest (T6).
- **T2 shipped** `migrations/2026-07-04_attribution_identity.sql` + schema mirror. Identity spine tables now exist in `shared/schema.ts`: `persons` (nullable primaryEmail/Phone/first/last — a person may exist before it's identified), `personIdentities` (kind email|phone|visitor + value; two unique indexes: `(kind,value)` global + `(personId,kind,value)`), `personMerges` (winnerId/loserId/reason audit — winner/loser are plain integers, NOT FKs, because a losing person may be deleted after merge). `analyticsEvents` gained: fbclid, gclid, clickId, fbp, fbc, personId, channel, channelRaw, landingUrl, isBot(bool default false). Reuse these types/tables downstream — don't redefine. T7 identity service writes here.
- Migration SQL convention in this repo: `integer PRIMARY KEY GENERATED ALWAYS AS IDENTITY`, `timestamp NOT NULL DEFAULT now()`, lowercase snake_case, additive verbs only (`CREATE TABLE IF NOT EXISTS`, `ADD COLUMN IF NOT EXISTS`, `CREATE UNIQUE INDEX IF NOT EXISTS`). Header comment says "Run on Supabase prod BEFORE the Fly deploy. DO NOT db:push." Matches `migrations/2026-07-03_league_waitlist.sql`.
- Drizzle table-extra config (indexes/unique) goes in the 3rd arg `(t) => ({ name: uniqueIndex("...").on(t.col) })` — see the `person_identities` def and existing `rewardBuilders`. `boolean(...).notNull().default(false)` mirrors SQL `boolean NOT NULL DEFAULT false`.
- Non-unique drizzle indexes need `index` from `drizzle-orm/pg-core` — it was NOT in schema.ts's import until T3 (only `uniqueIndex`/`unique`). Added it. Use `index("name").on(t.a, t.b)`.
- **T3 shipped** `migrations/2026-07-04_attribution_links_spend.sql` + schema mirror. New tables (reuse, don't redefine): `shortLinks` (org-scoped, `key` unique via `/l/:key`, cached counters clicks/leads/sales/saleAmountCents, `destination` must be allowlisted at create time — T9/T10), `linkClicks` (`clickId` unique, `ipHash`=SHA256(ip+ua) NO raw IP, 1h dedupe key), `adSpendDaily` (unique key `(date,adId,publisherPlatform,platformPosition)` — the two breakdown cols default `''` NOT null so the upsert key never has a NULL; `spendCents` = integer cents, Meta returns decimal dollars so T16 must ×100), `adEntities` (`adId` unique name-lookup). Insert schemas + types exported for all 4.
- **T4 shipped** `server/attribution-cookies.ts` — the first-party cookie spine. Reuse this surface downstream (don't reimplement): `parseCookieHeader(header)` → name→value map (tolerant, never throws), `isValidVisitorId(v)` / `isValidClickId(v)` (url-safe charset guard + `validExternalId` — use these before trusting ANY id read off a cookie or `?ci=`; they block Set-Cookie/header injection), `decideAttributionCookies({cookies, ciParam, mintVisitorId})` → `{visitorId, clickId, setCookies}`, `serializeSetCookie(spec, {secure})`, consts `VID_COOKIE`='usg_vid'/`CID_COOKIE`='usg_cid'/`VID_MAX_AGE_SECONDS`(2y)/`CID_MAX_AGE_SECONDS`(90d). The middleware hangs `req.usgVid`/`req.usgCid` off the request for downstream handlers (T8 conversion stamping reads these).
- **Middleware mounting:** `app.use(attributionCookieMiddleware)` sits in `server/index.ts` AFTER the request-logging middleware, BEFORE the async IIFE that calls `registerRoutes` — so it runs before every route. It only acts on non-`/api`, non-static-asset GET requests (STATIC_ASSET_RE skip list) → Set-Cookie lands on real HTML page loads only, not on JSON/asset fetches. Uses `res.append("Set-Cookie", …)` (not `res.set`) so it never clobbers a session Set-Cookie on the same response.
- **Secure flag:** app has NO `cookie-parser` (express-session parses its own). We parse cookies ourselves from `req.headers.cookie`. Set-Cookie `Secure` is keyed off `req.secure || x-forwarded-proto === 'https'` (session cookie hardcodes `secure:false`), so cookies still stick over local http dev yet get Secure behind Fly's https proxy. SameSite=Lax, NOT HttpOnly (analytics.js reads them). No `Domain` attribute (host-only) — cross-root sharing is T12/T13's job, not T4's.
- T4/T13 both touch the cookie middleware: T13 extends `decideAttributionCookies` for `?vi=` adopt/alias — keep that logic pure and in this same module so its tests live in `script/test-attribution-spine.ts` too.
- **T5 shipped** `client/public/analytics.js` upgraded (plain static file, NOT bundled/typechecked — vite just copies `client/public/` → `dist/public/`, so "build green" is trivially satisfied; validate JS separately with `node --check client/public/analytics.js` and grep for `=>`/`const`/`let`/backticks to keep it ES5-safe). It now emits these NEW fields on `session_start`/`page_view` events (T6 collector ingest must accept them): `visitorId` (now sourced from the `usg_vid` cookie, falling back to legacy `_cufc_vid`), `legacyVid` (the old id, sent ONCE per browser for stitching — guarded by `_cufc_legacy_sent` localStorage flag), `fbclid`, `gclid`, `ci`, `clickId` (=`ci` param || `usg_cid` cookie), `fbp` (`_fbp` cookie), `fbc` (`_fbc` cookie), `utmContent`, `utmTerm`, `landingUrl`. Existing top-level `utmSource/utmMedium/utmCampaign` stay on every event. Attribution fields are attached ONLY to session_start/page_view (via `applyAttribution()`); click/scroll/exit events stay lean — so T6 should classify touch from session_start/page_view rows. Cookies read via a new tolerant `getCookie()`; the client NEVER writes usg_vid/usg_cid (server owns them). `clickId` sends the last-click id even on later organic page views (usg_cid is a 90d sliding cookie) — that's intended last-click behaviour.
- **T6 shipped** collector ingest is now shaping, not a raw passthrough. New pure surface in `shared/attribution.ts` (reuse downstream, don't reimplement): `shapeAnalyticsEvent(raw, ctx?) → ShapedAnalyticsEvent | null` (null = malformed → dropped), `shapeAnalyticsEvents(rawArr, ctx?, limit=50)`, `isBotUserAgent(ua)` (missing/empty UA ⇒ bot; explicit isbot token list — deliberately NO bare `bot` substring so device brand "CUBOT" isn't false-flagged, uses `\bbot\b` for the generic case), `detectBot({userAgent, webdriver})` (webdriver hint ORs with UA), const `TOUCH_EVENT_TYPES` = {session_start, page_view}. Both `/api/public/analytics/event` + `/batch` in routes.ts call these with `{ userAgent: req.headers["user-agent"] }`.
- **T6 gotchas for later tasks:** (a) `analytics_events` has NO metaAdId/utmContent/utmTerm columns (those live on conversion tables per T3) — the shaper stashes `utmContent`/`utmTerm` and the T5 stitch signal `legacyVid` into the `metadata` jsonb instead. **T7 reads `metadata->>'legacyVid'` for the legacy-id stitch.** (b) Touch is classified ONLY on session_start/page_view rows (only they carry the full fbclid/gclid/clickId bundle from analytics.js); other event types get `channel = null` → reporting joins channel by visitor/session, not per-row. (c) Existing analytics overview queries read `metadata->>'trafficSource'|'isNewVisitor'|'seconds'|'maxPercent'` — the shaper preserves client `metadata` verbatim and only ADDS keys, so those keep working. (d) analytics.js now sends `evt.webdriver` on touch events (consumed for bot detection, then discarded — not a column).
- **T7 shipped** the identity service, split pure/DB. **Pure logic in `shared/identity.ts`** (reuse downstream, don't reimplement): `normalizeEmail(raw)` (trim+lowercase, validExternalId guard, requires `x@y.z` shape → the canonical form for persons.primaryEmail AND every `kind='email'` identity — T8 must normalise before lookups), `normalizePhone(raw)` (digits+`+`, ≥5 digits), `normalizeIdentityValue(kind,value)` (one entry point keying person_identities), `decideVisitorBind({existingPersonId,targetPersonId})` → `{action:'bind'|'noop'|'blocked_auto_merge'|'invalid', reason, audit}`, `isUsableIdentity`. **DB layer in `server/identity.ts`** (T8 calls these): `getOrCreatePersonByEmail(email,{phone,firstName,lastName})→Person|null` (null on bad email; enriches missing name/phone but NEVER overwrites; auto-links a phone identity), `linkIdentity(personId,kind,value)` (onConflictDoNothing on the (kind,value) unique — never steals an existing handle), `bindVisitorToPerson(visitorId,personId)→VisitorBindDecision` (bind→also stitches; blocked→writes person_merges `blocked_auto_merge` with winner=existing owner, keeps both), `stitchVisitorHistory(visitorId,personId)→count` (UPDATE analytics_events SET person_id WHERE visitor_id AND person_id IS NULL, THEN bridges each distinct `metadata->>'legacyVid'` and stitches those rows too).
- **T7 gotchas:** (a) `server/*.ts` importing `./db` THROWS at module load without DATABASE_URL (Hard Rule 1) — so any pure logic a test needs MUST live in `shared/` (that's why T7's decisions are in `shared/identity.ts`, mirroring how T4's live in `server/attribution-cookies.ts` which has no db import). Never import a server file that pulls in `./db` from a test. (b) A plain anon→identified bind writes NO person_merges row (there's no losing person); only `blocked_auto_merge` audits. (c) `db.execute(sql\`…\`)` returns `{rows}` (node-postgres) — cast `res.rows as Array<{col}>`; drizzle `.update(t).set().where().returning({id:t.id})` then `.length` gives the affected-row count. (d) `@shared/…` path alias works in server files (see db.ts); tests use a relative `../shared/…` import.
- **T8 shipped** conversion stamping via one entry point `server/attribution-stamp.ts`: `buildConversionAttribution(req, {email,phone,firstName,lastName}) → ConversionAttribution` (the 10 T3 columns + resolved `personId`). It reads the stamp off cookies FIRST (usg_vid/usg_cid/_fbp/_fbc) then body fallback (`readAttributionStamp` — pure, no DB, handles nested `body.attribution`/`body.utm` blobs), classifies channel via `classifyTouch`, then (email present) `getOrCreatePersonByEmail` → `bindVisitorToPerson` → stitch — every path swallowed so a conversion is NEVER blocked. **Reuse this, don't reimplement.** Two usage shapes: (a) spread `...attribution` into a columned table's `.values({… , ...attribution } as any)` (all 8 T3 tables — 10 endpoints incl. registrations ×3), (b) call it and discard the return for identity-only binding on tables WITHOUT attribution columns (venue booking-group confirm → facilityBookings; the 3 cross-origin inbox contact forms → inboxMessages).
- **T8 gotchas:** (a) the T4 cookie middleware skips `/api` paths, so `req.usgVid` is NOT populated on conversion POSTs — the helper parses `req.headers.cookie` itself (do the same for any new POST-side attribution read). (b) Cross-origin forms (mfl/cic/cugc contact, cic7s) post with SameSite=Lax cookies NOT sent → visitorId is null → the bind is skipped, but the payer person is still created/enriched from email (links them when they later convert on a columned same-origin funnel). (c) league register / camp / class all write to the SAME `registrations` table (MFL teams live there too), so all three thread `...attribution` into it. (d) venue checkout writes `facilityBookings` and inbox forms write `inboxMessages` — neither has T3 attribution columns, so those get identity-only (no column stamp); if reporting later needs channel on a venue booking, that's a NEW migration, not T8.
- **T9 shipped** the `/l/:key` redirect. Pure logic in `shared/short-links.ts` (client-safe, NO node built-ins — T10/T11 reuse it): `isAllowedDestination(url)`/`hostIsOurs(host)` (open-redirect guard: exact/subdomain match over `CLUB_ROOT_DOMAINS` + `clubos.fly.dev` + `*.vercel.app` + localhost — use these in T10 create-time validation so the DB only ever holds allow-listed destinations), `buildRedirectUrl(dest, clickId, {channel,medium,campaign,content})` (sets `ci` authoritative + fills ABSENT utm_source/medium/campaign/content only — existing dest params win), `clickIdFromBytes(bytes)`/`bytesToBase64Url` (hand-rolled base64url, byte-identical to node's `Buffer.toString('base64url')` — verified in test; 16 bytes → 22 chars, matches the T4 CID cookie charset), `mainSiteForHost(host)` (unknown-key fallback: strips join./order./book./app./l./www. → brand root `https://…`; dev/preview → `DEFAULT_MAIN_SITE`), `ipHashSeed(ip,ua)`=`${ip}||${ua}` (the SHA256 dedupe-key input; server hashes it, stores digest only). Consts `CLUB_ROOT_DOMAINS`/`DEFAULT_MAIN_SITE`.
- **T9 route gotchas:** (a) registered as `app.get("/l/:key")` at the TOP of `registerRoutes` so it beats the SPA static catch-all (serveStatic mounts AFTER registerRoutes). (b) The T4 cookie middleware DOES run on `/l/:key` (non-/api GET, no file ext) → `req.usgVid` is set + a usg_vid Set-Cookie already appended; the route additionally `res.append`s a usg_cid Set-Cookie with the fresh clickId (last Set-Cookie of a name wins in browsers, so it overrides any old cid the middleware re-issued). (c) 1h dedupe = query link_clicks for same (linkId, ipHash, isBot=false, createdAt > now-1h) → reuse that clickId, no new row, no counter bump; bots never deduped/counted; counter bump uses `sql\`${shortLinks.clicks} + 1\``. (d) added `gt` to the drizzle-orm import and `shortLinks`/`linkClicks` to the schema import in routes.ts. (e) ip taken from x-forwarded-for[0] || req.ip || socket.remoteAddress (matches the existing esign/ip pattern).
- **T3 conversion columns:** all 8 conversion/lead tables (registrations, cugc_registrations, cugc_free_sessions, league_waitlist, cic7s_registrations, football_institute_applications, print_orders, booking_requests) gained `visitorId/clickId/personId/fbp/fbc/metaAdId/metaAdsetId/metaCampaignId/metaPlatform/attributionChannel` (+ `hdyhau` on 6). **HDYHAU column reuse:** `registrations.referralSource` and `cugc_registrations.heardVia` already hold the self-reported "how heard" value, so no `hdyhau` col was added there — T17/T18 must COALESCE the existing column for those two tables. `personId` is a plain `integer` (no FK) to match `analytics_events.person_id`. cugc tables keep their `attribution` jsonb blob AND now have the discrete columns (discrete cols are what joins/reporting use).
- **T10 shipped** the short-link admin API (routes.ts + storage.ts) — **T11's UI consumes exactly these**. Endpoints (all `requireAuth`, org-scoped via `workspaceOrg(req)` + `checkUserOrg(userId,orgId)`; mutations/stats also re-check the workspace org OWNS the link → no cross-org edits): `GET /api/admin/links?archived=1` → `ShortLinkWithClicks[]` (each = ShortLink + `last7dClicks`), `POST /api/admin/links` (body: destination/channel/medium/campaign/content/brand/note/qrDefault/optional `key`; 201 → ShortLink; 400 bad dest/channel/key, 409 key taken), `PATCH /api/admin/links/:id` (archive/restore via `active:boolean` + edit note/brand/campaign/medium/content/qrDefault/channel/destination; key+org+counters immutable), `GET /api/admin/links/:id/stats?days=30` → `{ link, ...ShortLinkStats }`.
- **T10 reuse surface** — helpers to import in T11 (client) and any new link path, NOT reimplement: from `@shared/short-links` `isAllowedDestination` (open-redirect guard — validate before POST), `isValidLinkKey`/`RESERVED_LINK_KEYS`/`LINK_KEY_ALPHABET` (custom-key rules), `linkKeyFromBytes` (server key gen). Route-local (routes.ts, not exported) `slugifyCampaign` (utm_campaign → lowercase `-`-slug), `validateLinkChannel` (against `CANONICAL_CHANNELS`), `linkStrOrNull`. Channel dropdown in the UI = `CANONICAL_CHANNELS` (T11's "locked channel dropdown from canonical vocab" is just these).
- **T11 shipped** the Links & QR admin tab (`client/src/pages/links.tsx`). Registered in ALL workspaces via THREE files, each with a uniform anchor: `shared/tabs.ts` + `client/src/components/app-sidebar.tsx` add `links` right after the `dashboard` entry (identical `dashboard` line in all 7 arrays → one `replace_all` each; sidebar needs `Link2` from lucide, NOT wouter's `Link`). App.tsx has 7 admin `<Switch>` blocks: 6 use 8-space indent (`<Route path="/admin/team".../>` then NotFound → one `replace_all` hit 6), but the DEFAULT camps switch is a top-level `return` at **6-space** indent → needs a separate edit. Always `grep -c 'path="/admin/links"'` = 7 to confirm you caught all.
- **T11 client gotchas:** (a) the default react-query `queryFn` does `queryKey.join("/")`, so for a URL with a query string give the query an explicit `queryFn` using `apiRequest("GET", url).then(r=>r.json())` (don't put `?archived=1` in the key). Workspace scoping is automatic — `apiRequest`/`getQueryFn` inject `X-Workspace-Slug` from `localStorage.clubos_workspace`, so admin pages never set it by hand. (b) Import client-safe shared logic ONLY: `CANONICAL_CHANNELS` from `@shared/attribution`, `isAllowedDestination`/`isValidLinkKey` from `@shared/short-links`, and `import type { ShortLink }` from `@shared/schema` (type-only = erased). NEVER import `server/storage.ts` into a client file for its `ShortLinkWithClicks`/`ShortLinkStats` types — redeclare the shape locally (`type ShortLinkWithClicks = ShortLink & { last7dClicks: number }`). (c) QR is `qrcode.react` v4 (already a dep) — named exports `QRCodeSVG`/`QRCodeCanvas`; v4 dropped `includeMargin` (use a white padding wrapper for the quiet zone instead) and props are `value/size/level/bgColor/fgColor`. PNG export = render a hidden hi-res `QRCodeCanvas` and `canvas.toDataURL("image/png")`; SVG export = `new XMLSerializer().serializeToString(svgEl)` → Blob. No external QR service. (d) WhatsApp: `wa.me` is NOT in the CLUB_ROOT_DOMAINS allow-list, so a wa.me URL can't be a stored short-link destination — the WhatsApp helper is a standalone direct-share `wa.me/<digits>?text=<msg>` generator (copy + QR), not a POST to /api/admin/links. Don't loosen the open-redirect allow-list to make it a short link.
- **T10 storage patterns** (storage.ts): `listShortLinks` batches the trailing-7d non-bot click count for ALL links in ONE grouped `inArray` query (don't N+1). `getShortLinkStats` = NZ-local daily series (`to_char(created_at AT TIME ZONE 'Pacific/Auckland','YYYY-MM-DD')` — never toISOString for NZ) + conversions matched by `click_id IN (link's non-bot minted ids)` across all 8 T3 tables via a single UNION-ALL CTE (`db.execute(sql\`with cids as (...) ...union all...\`)`, `sql.raw(tableName)` for the table, `convRes.rows` cast). `ShortLinkWithClicks`/`ShortLinkStats` types are exported from storage.ts (line ~422). Auto-key collision retry = 6 tries widening 7→8 chars.
- **T12 shipped** the cross-site embeddable tracker. **`GET /t.js`** (top of `registerRoutes`, before the SPA catch-all, on every brand domain) serves a ~2.6KB ES5 tracker built by pure **`renderTrackerScript({collectorBase,domains})` in `shared/tracker-script.ts`** — the route injects `collectorBase` = `${proto}://${req.headers.host}` (so the script, running in the marketing page's origin, knows the absolute funnel-domain URLs to POST to) and `domains` = `CLUB_ROOT_DOMAINS`. The TS SOURCE uses a template literal (compiled away by esbuild); only the RETURNED STRING must be ES5-safe — the test enforces no `=>`/`const`/`let`/backtick + `new Function(js)` syntax check + <6KB. Reuse `renderTrackerScript`, don't hand-roll a second tracker.
- **T12 first-party cookie trick:** minifootball.co.nz ↔ join.minifootball.co.nz are SAME-SITE (same registrable root) but cross-ORIGIN, so a SameSite=Lax cookie with `Domain=minifootball.co.nz` IS accepted + shared across both. **`POST /api/public/analytics/hello`** (defensive, never throws) sets usg_vid scoped to that brand root via new **`rootDomainForHost(host)`** (`shared/short-links.ts` → the matching `CLUB_ROOT_DOMAINS` entry, or **null** for app/preview/dev hosts → host-only cookie, since `Domain=vercel.app`/`localhost` would be rejected). It reuses the existing usg_vid cookie, else adopts a decorated `body.vi` (only when NO existing cookie — the full `?vi=` adopt/**alias-event** matrix is T13's job, do NOT duplicate it here), else mints a uuid; refreshes usg_cid from `body.ci`; returns `{visitorId,clickId}` so the tracker can decorate links immediately.
- **T12 CORS:** new **`isOurOrigin(origin)`** (`shared/short-links.ts`, reuses `hostIsOurs`) gates a route-local `setTrackerCors(req,res)` that reflects the request Origin + `Access-Control-Allow-Credentials: true` (needed so the first-party Set-Cookie sticks on the cross-origin fetch) + `Vary: Origin`. Applied (with `app.options` preflight) to `/api/public/analytics/hello` AND the existing collector `/api/public/analytics/event`+`/batch` (so the marketing-site touch can post cross-origin). The tracker sends a `page_view` touch (metadata `{trafficSource:'cross-site',crossSite:true}`) → shaped/classified by the T6 collector like any other.
- **T12 `serializeSetCookie` extension** (`server/attribution-cookies.ts`): added `{domain?, sameSite?}` opts — Domain inserted before SameSite, SameSite defaults to `Lax`. ADDITIVE: callers passing no domain (T4 middleware, T9 redirect) get byte-identical output, so the spine test's serialize assertions still pass. Pass `rootDomainForHost(host)` (may be null → omitted) as `domain` for any brand-root-shared cookie.
- **T12 tracker↔T13 handoff:** the tracker decorates outbound `<a>` links to our OTHER brand roots (skips the current page's own root — the shared cookie already covers it) with `?vi=<visitor>&ci=<click>`. T13 extends the T4 cookie middleware to ADOPT that `?vi=` on the destination funnel landing (GET) + record the alias event. T12 already threads `vi` into `/hello`; T13 owns the middleware side + alias records.
- **T13 shipped** cross-root acceptance. **Pure logic (server/attribution-cookies.ts, still DB-free):** `decideAttributionCookies` now takes `viParam?` and returns `alias: VisitorAlias | null` (`{keep, alias}`). Rule: valid existing `usg_vid` wins (kept, never overwritten); else adopt a valid decorated `?vi=`; else mint. When an existing id AND a *different* valid `?vi=` are both present → keep ours + emit an alias link. Invalid/macro/injection `?vi=` (via `isValidVisitorId`) is ignored; `?vi=` equal to our id → no alias. `viParam` read in the middleware via the existing `firstQueryValue(req.query?.vi)`. **DB write:** middleware fire-and-forgets `void import("./attribution-alias").then(m => m.recordVisitorAlias(spec, ctx)).catch(()=>{})` — the dynamic import keeps attribution-cookies.ts DB-free so the spine test can still load it (the test never calls the middleware, so the lazy import never evaluates → no DATABASE_URL throw). Follows the `await import("./storage")` pattern already in index.ts.
- **T13 alias event shape** (`server/attribution-alias.ts` → `recordVisitorAlias`): an `analytics_events` row `{ visitor_id: <kept id>, session_id: 'alias:<kept>', event_type: 'alias', metadata: { aliasVisitorId: <decorated origin id>, source: 'cross-root' } }`, channel null. `ALIAS_EVENT_TYPE = 'alias'` exported. No dedupe (middleware only fires when `?vi=` is on the URL = the first cross-root landing; stitch reads links with SELECT DISTINCT so a rare dup is harmless). Never throws. Reuse `recordVisitorAlias`, don't hand-roll another alias writer.
- **T14 shipped** mailer instrumentation. Reuse surface, don't reimplement: **`shared/email-attribution.ts`** (pure, client-safe) `instrumentEmailHtml(html, EmailUtmOptions)` / `instrumentUrl(url, opts)` — rewrite ONLY ours-domain (`hostIsOurs`), non-`/api` hrefs, idempotent per param (existing param wins, tolerant of `&amp;`), preserves `#fragment`; `slugifyEmailCampaign`; const `EMAIL_CI_PREFIX='emc'`. `EmailUtmOptions = {source?='email', medium, campaign?, ci?}`. **`server/email-token.ts`** (crypto+DB, lazy-imported so callers stay DB-free): `emailClickToken(orgId,campaignId,email)` = `emc`+base64url(HMAC-sha256(`orgId:campaignId:normEmail`)[:24]) (deterministic → re-sends reuse it; url-safe → rides usg_cid + passes isValidClickId); `recordEmailClickToken(...)` persists token→email in `email_click_tokens` (onConflictDoNothing) and returns the token; `resolveAndBindEmailClick(token, visitorId)` maps token→email→person and binds (swallowed). `email_click_tokens` table + migration `2026-07-04_attribution_email_tokens.sql` (iteration 1).
- **T14 wiring pattern (how instrumentation actually turns on):** `sendEmail(EmailParams)` applies `instrumentEmailHtml` ONLY when `params.utm` is set (opt-in, wrapped in try/catch — never blocks a send). So each customer-facing template opts in by adding `utm:` to its `sendEmail({...})` call: transactional = `{ medium: "transactional", campaign: "<stable-slug>" }` (NO ci — the payer is already known via T8 conversion stamping; there's no campaign row); broadcast senders (`sendLeagueBroadcastEmail`/`sendCicBroadcastEmail`) compute `ci = params.campaignId ? await recordEmailClickToken(orgId, campaignId, to) : null` per recipient and spread `...(params.campaignId ? { utm: { source:"email", medium:"broadcast", campaign: String(campaignId), ci } } : {})`. GOTCHA that bit this run: a broadcast sender can *accept* orgId/campaignId yet still send un-instrumented if (a) the route call site doesn't pass them (MFL `/api/admin/league/mailer/send` + CIC `/api/admin/cic/mailer/send` queue closures now pass `orgId`+`campaignId: campaign.id`), or (b) the sender computes `ci` but forgets the utm spread on its `sendEmail` (the CIC sender had exactly this bug). Grep `medium: "broadcast"` should = 2 (both senders), and each broadcast route closure must pass campaignId.
- **T14 click→identity bind** happens in the T4/T13 cookie middleware (`server/attribution-cookies.ts`): on a fresh `?ci=` landing where `ciParam.startsWith(EMAIL_CI_PREFIX) && isValidClickId(ciParam)`, it fire-and-forgets `void import("./email-token").then(m => m.resolveAndBindEmailClick(ciParam, decision.visitorId))`. Only the URL `?ci=` param counts as "fresh" (the usg_cid cookie carries the token forward on later page views but we bind once, on landing). The dynamic import keeps attribution-cookies.ts DB-free so `script/test-attribution-spine.ts` still loads it — same lazy-import trick as the T13 alias write. Importing `EMAIL_CI_PREFIX` from `../shared/email-attribution` is safe in that module (it only pulls `shared/short-links.ts`, both pure).
- **T14 which templates to instrument:** only customer-facing emails whose hrefs point at an **allow-listed** brand root (CLUB_ROOT_DOMAINS) get real instrumentation — everything else is a silent no-op (`instrumentUrl` passes non-ours + `/api` links through). This run: 4 MFL transactional confirmations (league-confirmation/league-team-confirmed/league-season-reward/mfl-waitlist, all link `join.minifootball.co.nz`). SKIPPED: booking-declined (`book.unitedsportscentre.com` NOT in CLUB_ROOT_DOMAINS → no-op; adding that domain is a T9 open-redirect decision, not T14), camp + CUGC confirmations (no funnel CTA links → nothing to rewrite), and all internal `*Notification` staff emails (not customer-facing). To find candidates fast: `grep -n 'href="https://' server/email.ts` then cross-check the host against CLUB_ROOT_DOMAINS.
- **WORKTREE GOTCHA (cost me several tool calls):** the loop runs in the git worktree at `apps/clubos/.worktrees/attribution` (branch `loop/attribution`), NOT the parent `apps/clubos` checkout (which is on `main`, older). Read/Edit/Grep with the PARENT path silently gives you the WRONG (main) file — e.g. meta-capi.ts showed `v19.0` there but `v23.0` here. ALWAYS use worktree-absolute paths `/…/.worktrees/attribution/…` (or `pwd`-relative in Bash, since cwd IS the worktree). `git log`/`ls` in Bash confirm you're on the right tree.
- **T16 shipped** the Meta ad-spend sync. Pure parsers/builders in **`shared/meta-insights.ts`** (client-safe, NO node builtins — reuse in T18/T19 reporting + any Marketing-API caller, don't reimplement): `normalizeAdAccountId`/`parseAdAccountIds` (→ `act_<digits>`, comma/space/semicolon/newline split, deduped), `dollarsToCents(spend)` (Meta returns decimal DOLLARS as strings → integer cents, `Math.round`, comma-tolerant), `toInt`, `nzDateString(d,tz='Pacific/Auckland')`/`trailingWindow(now,days=7)` (NZ-local, deterministic given the Date — NEVER toISOString for NZ), `buildInsightsUrl(acct,{since,until,apiVersion,limit})` + `buildAdEntityUrl(adId,{apiVersion})` (both TOKEN-FREE so they're testable — caller appends `access_token`; Insights = `level=ad&breakdowns=publisher_platform,platform_position&time_increment=1&time_range={"since","until"}`), `parseInsightsPage(json)→ParsedAdSpendRow[]` (skips rows w/o ad_id or date_start; breakdown fields default `''` to match the ad_spend_daily unique key), `insightsNextPage(json)` (paging.next cursor — ALREADY carries the token), `parseAdEntity(json,fallbackAdId?)` (node `/{ad-id}?fields=name,adset{name,campaign{name}}` → nested id/name extraction), `adEntitiesFromSpend(rows)` (collapse to one entity per ad — names ride the Insights rows so spent ads need NO node fetch). Const `META_GRAPH_BASE`, `INSIGHTS_FIELDS`.
- **T16 cron (`server/ad-spend-cron.ts`, print-cron/league-balance pattern):** `runAdSpendSync(now=new Date())` (never throws) → resolve accounts (env `META_AD_ACCOUNT_IDS`|`META_AD_ACCOUNT_ID` else `settings` row key `meta_ad_account_ids`) → paginate Insights per account (MAX_PAGES backstop; first URL gets token appended, cursors don't) → upsert `ad_spend_daily` via `onConflictDoUpdate` on `[date,adId,publisherPlatform,platformPosition]` → upsert `ad_entities` from spend → then node-fetch names ONLY for `meta_ad_id`s that appear in the 8 conversion tables (static UNION over `db.execute(sql.raw(...))`) but not in spend/ad_entities yet (`validExternalId`-hygiened so `{{ad.id}}` macros are dropped; capped `MAX_CONVERSION_ENTITY_FETCHES=100/run`). `startAdSpendCron()` guarded (`started` flag), daily interval (`AD_SPEND_CRON_INTERVAL_MS` override), first run +5min, **no-op + log without META_ACCESS_TOKEN or accounts**. Registered in `server/index.ts` after `startApiSecurityJobs`. `META_API_VERSION` is now `export`ed from `server/meta-capi.ts` (single v23.0 source shared with CAPI). Reporting (T18) reads names by joining `ad_entities.ad_id` and CAC/ROAS by joining `ad_spend_daily` (spend_cents ÷ 100 for dollars). Test: `npx tsx script/test-attribution-meta.ts` (94 assertions, no live call).
- **T13 extended `stitchVisitorHistory` (server/identity.ts):** the legacy-id bridge became a single grouped UNION query that also follows cross-root aliases in BOTH directions — `metadata->>'legacyVid'` (rows under this visitor) ∪ `metadata->>'aliasVisitorId'` where `event_type='alias' AND visitor_id=X` (forward) ∪ `visitor_id` where `event_type='alias' AND metadata->>'aliasVisitorId'=X` (reverse) → stitch each bridged id's still-anonymous rows. One level (no recursion), inside the existing try/catch. So a person who converts on EITHER brand root gets both spines stitched. T18 reporting reads channel off touch rows (session_start/page_view), so alias rows (channel null) don't pollute channel aggregates. **T21 prune note:** T21 must NOT delete `event_type='alias'` rows (they're linking records, not page_view/scroll) — its rule already targets only page_view/scroll so this is safe, but keep it in mind.
- **T19 shipped** the admin reporting API — 7 GET endpoints under `/api/admin/attribution/*` (overview/campaigns/ads/leads/reconciliation/journeys/journey/:personId), each `requireAuth`, all in routes.ts right after the T10 links block. Two route-local helpers (define once, reuse in T20 if it ever needs a raw call): **`attributionScope(req)`** → `{ok:true, org, orgIds, group}` | `{ok:false, status, message}` (checks `workspaceOrg` + `checkUserOrg`; default `orgIds=[org.id]`, but when `?group=1|true` AND `workspaceTypeFor(org.slug)==='group'` it rolls up `storage.getUserOrganizations(userId)` — a NON-group workspace can never widen its scope, that's the security boundary) and **`parseAttributionParams(req, orgIds)`** → `ReportParams` (model, windowDays, conversion range from explicit `start`/`end` OR a trailing `days` count, filter). Every response is `res.json({ group: scope.group, ...data })` so the UI knows if it's a rollup.
- **T19 gotchas:** (a) `req.params.x` is typed `string | string[]` in this repo's Express types — `parseInt(req.params.personId)` is a NEW tsc error; use `parseInt(String(req.params.personId), 10)`. `req.query.x as string` is the established cast for query params (see the stats endpoint). (b) `personJourney` (T18) loads the person row + touch log WITHOUT an org filter (analytics_events/persons have no org column) — only its conversions are org-scoped, so a raw call leaks PII across workspaces. The endpoint closes this by treating `data.conversionCount === 0` as 404 (a person only "belongs" to your workspace if they converted in one of your scoped orgs; that's exactly the set the journeys list surfaces). (c) I added ONE new read-only builder to `server/attribution-reports.ts` — `recentConversions(params & {limit})` (fetchConversions in range → tagNewVsReturning → sort desc → cap ≤500, anonymous rows kept with `personId:null`) to back the `journeys` LIST endpoint (T18 had no list builder). It's DB glue like the rest of that file → not unit-testable in-loop; the pure model layer test (`script/test-attribution-reports.ts`) still passes 62. (d) `checkUserOrg`/`workspaceOrg` are function declarations inside `registerRoutes` — hoisted, so callable from the earlier T19 block even though `checkUserOrg` is defined ~1400 lines later (the T10 links endpoints already rely on this).
- **T17 shipped** HDYHAU capture. Reuse surface: shared vocab **`HDYHAU_OPTIONS`** (8 classifier-aligned `{id,label,channel}` — id stored verbatim, maps via `mapHdyhauToChannel`; guard: `script/test-attribution-hdyhau.ts`) + `normalizeHdyhauAnswer` in `shared/attribution.ts`. Generalized **`POST /api/public/hdyhau`** `{type,id,value}` (dispatch: `registration`→`updateRegistration({referralSource})`, `waitlist`→`updateLeagueWaitlistEntry({hdyhau})`, `booking_request`→`db.update(bookingRequests)`; best-effort — any failure returns 200`{ok:false}` so the tap never errors). Reusable **`client/src/components/hdyhau-card.tsx`** `<HdyhauCard type id variant accent answered/>` — one-tap (tap=submit, fire-and-forget fetch), skippable, fires once via localStorage `usg_hdyhau_<type>_<id>`, dark/light theme; renders null when id missing/invalid or `answered`. Mounts: mfl-success (registration, `answered={!!reg.referralSource}`), booking-success/camp (registration, light — `answered` suppresses double-ask with the existing `/feedback` AttributionSurvey), mfl-waitlist (captured `body.id`), member-booking request-success (`booking_request`, `submittedId`). **Venue note:** the paid `facilityBookings` flow (venue-book-success) has NO hdyhau column per T3 → the venue mount is `booking_requests` (member-booking), which owns hdyhau. `react-icons` `IconType` imports from the pkg root; `SiWhatsapp` exists.
