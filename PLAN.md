# Plan — warehouse (WMS inside ClubOS, United Prints workspace)

> The loop works the **top-most unchecked box** each run, then commits.
> Full architecture in `SPEC.md` (read the referenced section first). Conventions in `AGENTS.md`.
> Every task must end with `npm run check` clean, plus its own `verify:` line.

## Tasks

- [x] T1 — see Done section
- [x] T2 — see Done section
- [x] T3 — see Done section
- [x] T4 — see Done section
- [x] T5 — see Done section
- [x] T6 — see Done section
- [x] T7 — see Done section
- [x] T8 — see Done section
- [x] T9 — see Done section
- [x] T10 — see Done section
- [x] T11 — see Done section
- [x] T12 — see Done section
- [x] T13 — see Done section
- [x] T14 — see Done section
- [x] T15 — see Done section
- [x] T16a — see Done section
- [x] T16b — see Done section
- [x] T16c — see Done section
- [x] T17 — see Done section
- [x] T18 — see Done section
- [x] T19 — see Done section
- [x] T20 — see Done section

## Done

<!-- completed tasks move here with a one-line note from the agent -->
- [x] T1 — `shared/warehouse.ts` (finished a partial file left by a crashed run: fixed 2 tsc errors — `new Set([...])` needs an explicit type param `new Set<ReasonCode>([...])` / `new Set<RequisitionStatus>([...])` or it infers `Set<string>` and won't assign to a `ReadonlySet<T>` field). All D15 taxonomy + item/brand/unit/location/reservation/PO/requisition/loan/count enums + validators + SKU/location/alias normalisers + stock math (`availableQty`, `wouldGoNegative`, `legsSumToZero`) + derived helpers (`dispositionForReason`, `isSellableLocation`, `isLoanOverdue`, `countLineNeedsRecount`) were already present and matched SPEC §4.1/D7/D8/D12/D15 verbatim. Added `script/test-warehouse-shared.ts` (114 assertions, one per exported const/function incl. edge cases: truncation, ambiguous-char SKUs, quarantine routing, terminal states, missing-cost variance rule, empty leg lists). verify: `npx tsx script/test-warehouse-shared.ts` (114 passed) && `bash script/check-warehouse-gate.sh` (PASS).
- [x] T2 — All 16 `wh_*` Drizzle tables added to `shared/schema.ts` (§4.1): wh_locations, wh_items, wh_barcode_aliases, wh_movements (the ledger, `delta<>0` CHECK), wh_stock, wh_reservations, wh_purchase_orders + wh_po_lines, wh_requisitions + wh_requisition_lines, wh_loans + wh_loan_lines, wh_counts + wh_count_lines, wh_shopify_events, wh_sync_state — not org-scoped (single shared warehouse, like feature_requests), no DB CHECKs on enum-ish columns, no forward-ref issues (ordered locations→items→everything else). `migrations/2026-07-13_warehouse.sql` (additive, `IF NOT EXISTS` everywhere, 11 named unique/partial-unique indexes + the one true `delta<>0` CHECK). `script/apply-warehouse.ts` (dry-run by default — BEGIN→migrate→verify 16 tables + 11 indexes (asserting partial ones actually carry WHERE) + the CHECK constraint→ROLLBACK; `--apply` commits instead; confirmed it refuses to run with no `DATABASE_URL` — never opened a DB connection). verify: `bash script/check-warehouse-gate.sh` (PASS) && `npm run build` (exit 0) — cross-checked apply script's arrays against the migration file with a throwaway node script (all 16 tables + 11 indexes match by name, no drift).
- [x] T3 — `server/warehouse.ts`: the movement engine. `postMovementGroup(tx, input)` per SPEC §4.2 — validates the taxonomy first (no DB touched on a bad payload), idempotency short-circuits BEFORE any write (only the first leg's row carries the unique `idempotency_key`, replay returns the prior `groupId` untouched), inserts one ledger row per leg sharing a fresh `groupId`, then runs one atomic guarded upsert per leg against `wh_stock` (guard skipped for `allow_negative` items or a genuinely first-ever row for that (item,location) — matches real `INSERT...ON CONFLICT...DO UPDATE...WHERE` semantics where the guard only fires on the conflict path); any leg's guard failing throws `InsufficientStockError` naming the location, which rolls back the WHOLE group once wrapped in a real `db.transaction()`. `runMovementGroup()` is the convenience wrapper routes will call (opens its own tx, fires post-commit `onMovementCommitted` hooks for T12's future channel-sync push — never fired on a replay). `reconcileStock()` is the nightly D1 job: compares `wh_stock.on_hand` against `SUM(wh_movements.delta)` per (item,location) and repairs+reports any drift. Both engine functions take a bespoke `WarehouseDb`/`ReconcileDb` seam (NOT a `Pick<typeof realDb,...>` like T2's precedent — see the file-header comment for why: a computed guarded upsert and a full-outer-join aggregate aren't practically fakeable via drizzle's raw query-builder chain); the real adapters (`warehouseDbFromTx`, `reconcileDbFromRealDb`) use genuine drizzle calls, type-checked against schema.ts but never unit-tested directly (need a live Postgres). `script/test-warehouse-engine.ts` (17 assertions incl. all 4 PLAN-required behaviours: idempotent replay untouched + no double-write, delta-0 rejected pre-DB, insufficient stock on any leg throws naming the right location, transfer legs summing to zero both required and enforced) + validation edge cases + allow_negative bypass + leg-level reasonCode override + reconcile drift/no-drift. `server/db.ts` throws at import if `DATABASE_URL` is unset, so `db` is only ever reached via `import type` (erased) + a lazy `await import("./db")` inside `runMovementGroup`/`reconcileStock`'s bodies — the test script never touches it. verify: `npx tsx script/test-warehouse-engine.ts` (17 passed) && `bash script/check-warehouse-gate.sh` (PASS) && `npm run build` (exit 0, not required by AGENTS.md for a non-client task but run as a sanity check).
- [x] T4 — `server/warehouse-routes.ts` (self-contained, direct `db` calls — NOT storage.ts; follows the `feedback-routes.ts`/`market-research-routes.ts` standalone-module convention, not `shop-routes.ts`'s storage-layer one, since there's no existing warehouse storage layer to extend and this keeps the diff isolated): full CRUD + `q` search for `wh_locations` (code/kind/active filters, zone always server-derived via new `deriveLocationZone()` — never a client-editable field, so it can't drift from the code) and `wh_items` (sku/name/category search, kind/brandOwner/active filters, joins in `defaultLocationCode`, detail view nests its aliases); `wh_barcode_aliases` CRUD nested under `/items/:itemId/aliases` + a flat searchable `/aliases?q=&itemId=` list joined to sku/name. Label-payload endpoints `GET /labels/items?ids=` (payload = the SKU verbatim) and `GET /labels/locations?ids=` (payload = new `locationBarcodePayload()` helper, `LOC:` + code) — bulk id lookups for T14's client-side QR rendering, never generate an image server-side. Added `deriveLocationZone()` + `LOCATION_BARCODE_PREFIX`/`locationBarcodePayload()` to `shared/warehouse.ts` (pure logic, no DB) with 7 new assertions in `test-warehouse-shared.ts` (121 total) to keep the "test every export" discipline from T1. Registered in `server/index.ts` via the same dynamic-import pattern as `registerFeedbackRoutes` (not literally "next to shop routes" as AGENTS.md's wording suggested — shop routes actually register inside `routes.ts`, not `index.ts`; the feedback/market-research/club-dossier standalone-module registration point in `index.ts` is what T4's own line meant and is the right place). Postgres 23505 (unique) / 23503 (FK-restrict) errors are caught and returned as clean 409s rather than raw 500s (mirrors `shop-routes.ts`'s `handleShopError` pattern). No DB CHECKs; enum-ish fields (kind/brandOwner/unit/locationKind) validated against `shared/warehouse.ts`; numeric(12,3) columns (`purchaseQty`/`minQty`/`packQty`) converted explicitly string↔number at the route edge; `costCents` validated as a whole integer. verify: `bash script/check-warehouse-gate.sh` (PASS) && `npx tsx script/test-warehouse-shared.ts` (121 passed) && `npx tsx script/test-warehouse-engine.ts` (17 passed, unaffected) && `npm run build` (exit 0).
- [x] T5 — Reservations, added to `server/warehouse.ts`: `reserveStock`/`releaseReservation`/`releaseReservationsForRef`/`consumeReservation` + a bespoke `ReservationDb` seam (same reasoning as T3's `WarehouseDb`/`ReconcileDb` — `getAvailableForItem` needs two row-LOCKED SELECTs, not one aggregate, since Postgres disallows `FOR UPDATE` with GROUP BY; real adapter `reservationDbFromTx`, never unit-tested directly). `wh_reservations` is ITEM-level (no location_id — confirmed against schema.ts), so `available(item)` = Σ on_hand across sellable bins (`kind <> 'virtual' AND UPPER(code) <> 'QUARANTINE'`, mirroring `isSellableLocation`) − Σ active reservations' qty. `reserveStock` is idempotent per (refKind,refId,itemId) — a second call for the same ref+item returns the existing reservation WITHOUT re-checking availability (mirrors postMovementGroup's idempotency contract); insufficient availability throws the new `InsufficientAvailableError` (item-level sibling of T3's location-level `InsufficientStockError`). `releaseReservation` is idempotent (releasing an already-released reservation is a no-op; releasing a consumed one throws — stock already left). `consumeReservation` always consumes the reservation's FULL qty (no partial-ship state in the schema — a deliberate v1 scope cut, noted inline) — posts the movement via `postMovementGroup` (so the LOCATION-level guard still applies even though the ITEM-level availability check already passed — reserving against total item availability can't know in advance which physical bin a dispatch scan will actually name) then marks the reservation `consumed`, but ONLY when the movement wasn't an idempotent replay (mirrors T3's "replay touches nothing else" contract one level up). Route-facing `runReserveStock`/`runReleaseReservation`/`runReleaseReservationsForRef`/`runConsumeReservation` wrappers open their own transaction (lazy `db` import, same as `runMovementGroup`) and fire the existing `notifyMovementCommitted` sync hook — but only on a genuine change, never a no-op/replay. New HTTP routes in `server/warehouse-routes.ts`: `GET /items/:id/available`, `GET/POST /reservations`, `POST /reservations/:id/release`, `POST /reservations/:id/consume` — `handleWarehouseError` extended to turn `InsufficientStockError`/`InsufficientAvailableError` into clean 409s. 15 new assertions in `test-warehouse-engine.ts` (32 total) via one combined `WarehouseDb & ReservationDb` fake (shares one stock map so a consume's decrement is visible to a later availability read) covering: available dropping/restoring, quarantine/virtual exclusion, insufficient-availability, idempotent reserve/consume replays (asserting the underlying calls do NOT re-run), release-idempotency + consumed-can't-release, release-by-ref (mixed statuses, wrong ref untouched), and the location-level guard firing despite sufficient item-level availability. verify: `npx tsx script/test-warehouse-engine.ts` (32 passed) && `bash script/check-warehouse-gate.sh` (PASS) && `npx tsx script/test-warehouse-shared.ts` (121 passed, unaffected) && `npm run build` (exit 0).
- [x] T6 — Purchase orders + receiving, all in `server/warehouse-routes.ts` (master-data CRUD, same doctrine as T4 — never touches `wh_movements`/`wh_stock` except the one receive endpoint): full CRUD for `wh_purchase_orders` (list w/ q/status filters + a merged per-PO line-count/total-ordered-qty aggregate, detail w/ derived lines, create, patch, delete-blocked-once-anything's-been-received) and `wh_po_lines` (nested list/create under `/pos/:poId/lines`, flat patch/delete under `/pos/lines/:id`, both blocked once a line has receiving history — editing/deleting a quantity that's already been physically received would make the ledger's reference stale). `POST /pos/:poId/receive` posts ONE movement group (`runMovementGroup`, movementType `receipt`, ref `{kind:'po', id: poLineId}`) with up to two legs — `qtyGood` → the named sellable location, `qtyDamaged` → the `QUARANTINE` location (`reasonCode:'damaged'`, the only D15 reason that applies at receiving) — then recomputes the PO's status from every line's derived total (never touches `draft`/`cancelled`/`closed`). Interpreted "over/short/damaged → discrepancy note" literally against D15's actual reason-code taxonomy (no code exists for "unexpected surplus"): only damaged stock is quarantined; over/short are compared against an `expectedQty` (defaults to the line's outstanding qty, derived from prior receipts; a packing-slip override is accepted) purely to produce a human-readable note on the movement group, never extra routing — documented inline so a future task doesn't "fix" this into inventing a new reason code. `qty_received` is derived via a raw `SUM(delta) FILTER (WHERE reason_code ...)` aggregate split into good/damaged (`getPoLineReceiptTotals`, `loadPoLinesWithReceipts` — shared by the detail GET and the receive endpoint's own status recompute so the two can never disagree on what "received" means) — same "aggregate isn't practically expressible via drizzle's builder" reasoning as T3's reconcile query. Added to `shared/warehouse.ts`: `isValidDateOnly` (YYYY-MM-DD shape, reusable by T9/T10's date fields later), `computeReceiveDiscrepancy`/`receiveDiscrepancyNote`, `derivePoStatusFromLines` — 27 new assertions in `test-warehouse-shared.ts` (144 total). verify: `bash script/check-warehouse-gate.sh` (PASS) && `npx tsx script/test-warehouse-shared.ts` (144 passed) && `npx tsx script/test-warehouse-engine.ts` (32 passed, unaffected) && `npm run build` (exit 0).
- [x] T7 — Scan resolver + putaway/transfer. `resolveScanCode(db, rawCode)` in `server/warehouse.ts` behind a bespoke `ScanLookupDb` seam (three simple unlocked lookups — findItemBySku/findAliasByCode/findLocationByCode — same "seam for DB-free testing" reasoning as WarehouseDb/ReservationDb, even though nothing here needs a row lock): resolution order is (1) `LOC:`-prefixed → location only, a miss is `unknown` and NEVER falls through to item/alias lookups (`stripLocationPrefix` in `shared/warehouse.ts` marks the code unambiguous); (2) our own SKU scheme via `normaliseSku`; (3) a barcode alias looked up verbatim (`normaliseAliasCode`, trim-only — never uppercased, since we don't own that format) returning the alias's own `packQty` (so `scanQuantityToUnits(scans, packQty)` gives the true unit count — a case-of-6 scanned 4 times posts 24, never 4); (4) a bare (unprefixed) location code for an operator typing a bin code by hand. Every branch returns `actions: MovementType[]` via `scanActionsForItem`/`scanActionsForLocation` (loan actions only for `isLoanable` items; virtual locations get zero actions — nobody scans a printed label for SUPPLIER/CUSTOMER/SCRAP/PRODUCTION). Real adapter `scanLookupDbFromDb` — plain reads, never unit-tested directly (consistent with the rest of the file, though this one doesn't strictly need a live Postgres to be trustworthy). Putaway + transfer share one route handler `handleLocationMove` in `server/warehouse-routes.ts`: loads the item + both locations (rejecting a virtual location as either endpoint — `resolveRealLocationForMove` — and rejecting `fromLocationId === toLocationId`), builds the opposite-signed pair via `shared/warehouse.ts`'s `buildLocationMoveLegs`, and posts them through the existing T3 `runMovementGroup` — no new engine machinery, only the `movementType` label ("putaway" vs "transfer") differs. New routes: `POST /scan` (pure lookup, never mutates), `POST /putaway`, `POST /transfer`. Found this task's code (shared helpers + engine function + routes + a 20-assertion `script/test-warehouse-scan.ts`) already written but uncommitted from a crashed prior run — verified it against SPEC §4.4/D5/D7/D8 line by line rather than rewriting, ran the full test/gate suite fresh, and it was already correct; only PLAN.md's own checkbox and this Done note were still outstanding. verify: `npx tsx script/test-warehouse-scan.ts` (20 passed — alias pack_qty multiplication, LOC:-prefix no-fallthrough, unknown-code path, putaway/transfer through the real engine incl. insufficient-stock and allow_negative) && `bash script/check-warehouse-gate.sh` (PASS) && `npx tsx script/test-warehouse-shared.ts` (158 passed, unaffected) && `npx tsx script/test-warehouse-engine.ts` (32 passed, unaffected).
- [x] T7 — Scan resolver + putaway/transfer. `resolveScanCode` (`server/warehouse.ts`) is the one place a scanned/typed code becomes `item | location | unknown`, behind a bespoke `ScanLookupDb` seam (three plain unlocked lookups — no row-locking needed here unlike T5's `ReservationDb`, kept bespoke purely so the DB-free test can fake it) with a real adapter `scanLookupDbFromDb`. Resolution order, first match wins: (1) `LOC:`-prefixed → location only, a miss is `unknown` and NEVER falls through to item/alias (a mistyped bin code must not get silently checked against SKUs it was never meant to be); (2) our own SKU scheme (D7, we own the format — checked before aliases); (3) a barcode alias looked up verbatim, resolving with that alias's own `packQty` (vs. `packQty:1` for a direct SKU match); (4) a bare unprefixed location code (an operator typing a bin code by hand); (5) `unknown` (raw input preserved untrimmed). Added to `shared/warehouse.ts` (DB-free, tested in `test-warehouse-shared.ts` per its "test every export" discipline): `stripLocationPrefix`, `scanActionsForItem`/`scanActionsForLocation` (the scan station's advisory action-sheet lists — virtual locations offer nothing since nobody scans a printed SUPPLIER/SCRAP label), `scanQuantityToUnits` (the pack_qty multiplier arithmetic), `buildLocationMoveLegs` (the shared two-leg shape putaway and transfer both use — only `movementType` differs between the two route handlers, `handleLocationMove` in `server/warehouse-routes.ts`). New routes: `POST /scan` (body `{code}` → `ScanResolution`), `POST /putaway`, `POST /transfer` (body `{itemId, fromLocationId, toLocationId, qty, note?, idempotencyKey?}` — both locations must be real, non-virtual, and different; posted via `runMovementGroup`). New `script/test-warehouse-scan.ts` (20 assertions): LOC:-prefix hit/case-insensitive/miss-never-falls-through, SKU match priority over a same-keyed alias, alias match + pack_qty multiplication via `scanQuantityToUnits`, alias verbatim (non-uppercased) lookup, bare-location fallback + its own priority-after-item ordering, empty/unknown-code paths (with call-count assertions proving no wasted lookups), and an end-to-end proof that `buildLocationMoveLegs`' legs actually move stock (incl. reject-on-insufficient-stock and `allow_negative` bypass) through the already-tested `postMovementGroup`. verify: `bash script/check-warehouse-gate.sh` (PASS) && `npx tsx script/test-warehouse-scan.ts` (20 passed) && `npx tsx script/test-warehouse-shared.ts` (158 passed) && `npx tsx script/test-warehouse-engine.ts` (32 passed, unaffected) && `npm run build` (exit 0).
- [x] T8 — Pick queue + dispatch. Found this task's code already written (an auto-checkpoint commit `loop(warehouse): iteration 3` sitting on top of T7, `DISPATCH_SOURCE_KINDS`/`isDispatchSourceKind`/`nextOrderStatusAfterDispatch` in `shared/warehouse.ts` + the full `GET /pick-queue` + `POST /dispatch` route pair in `server/warehouse-routes.ts`) — verified it against SPEC/PLAN line by line (same "don't assume unstarted, read what's there first" move as T7) rather than redoing it under a different name; my own first-draft `PICK_SOURCES`/`isPickSource` duplicate was removed once the existing `DISPATCH_SOURCE_KINDS` was confirmed correct and complete. `GET /pick-queue` unions three sources into `{nativeOrders, shopifyOrders, requisitions}`: paid native `shop_orders` joined to WMS-mapped variants (`whItems.shopVariantId`) with already-**consumed** `shop_order` reservations filtered out (self-referential — see below); active `wh_reservations` rows with `ref_kind='shopify_order'` grouped by the Shopify order id (the ONLY local trace of a Shopify order — we hold no order table for it); `approved` requisitions with `qtyRequested − qtyPicked > 0` lines. `POST /dispatch` (`{sourceKind, sourceId, itemId, locationId, qty?, idempotencyKey?, note?}`): **requisition** posts a plain `pick` movement (ref `{kind:'requisition', id: requisitionLineId}`) and increments `qtyPicked` (only on a genuinely new movement, mirroring the replay-touches-nothing-else contract) — hard-caps at what's remaining; **shopify_order** requires an existing active reservation (created by T12's future webhook path, or manually via T5's endpoint today) and consumes it via `runConsumeReservation`, then — only on a real (non-replay) consume — calls a stubbed `notifyShopifyFulfilled()` gated behind `WH_SHOPIFY_FULFIL=1` (T12 owns the real GraphQL `fulfillmentCreate` call, `// TODO-verify(live)`); **shop_order** has no reservation yet pre-T13, so it calls `runReserveStock` (idempotent) then `runConsumeReservation` in the same request — backfilling at dispatch time what T13 will one day do at payment time — and this consumed reservation is exactly what makes the pick-queue's own "already dispatched" filter self-consistent (no separate "dispatched" flag needed anywhere). "Mark source" for a fully-dispatched native order advances `shop_orders.status` past `paid` into `shop-routes.ts`'s own existing `ADMIN_SETTABLE_STATUSES` pipeline (`shipped` if `addressLine1` is set, else `ready_for_pickup` — confirmed this admin-status-set path has no side effects beyond the field write, so a direct `db.update` from warehouse-routes.ts is safe) — a partial dispatch leaves it `paid` so remaining lines keep surfacing. Fixed two real gaps found while verifying: (1) the requisition leg never fetched/passed the item's own `allowNegative` (D16) — a bulk material with `allow_negative=true` would have wrongly hit the non-negative guard on an internal draw; (2) the requisition dispatch branch never checked the parent requisition's own `status`, so stock could physically leave the building against a `submitted`(-not-yet-approved)/`declined` requisition — added the same status-gate style as T6's `RECEIVABLE_PO_STATUSES`. Also simplified `nextOrderStatusAfterDispatch`'s call site to reuse `shop-routes.ts`'s own established `!!order.addressLine1` convention (used in 3 other places there) instead of an extra `shop_shipping_options` join, dropping that now-unused import. Added `isDispatchSourceKind`/`nextOrderStatusAfterDispatch` assertions to `test-warehouse-shared.ts` (162 total) to keep the "test every export" discipline. verify: `bash script/check-warehouse-gate.sh` (PASS) && `npx tsx script/test-warehouse-shared.ts` (162 passed) && `npx tsx script/test-warehouse-engine.ts` (32 passed, unaffected) && `npx tsx script/test-warehouse-scan.ts` (20 passed, unaffected) && `npm run build` (exit 0).
- [x] T9 — Requisitions end-to-end. Found an auto-checkpoint commit (`iteration 6`) already sitting on top of T8 with a well-formed transition graph in `shared/warehouse.ts` (`REQUISITION_TRANSITIONS`/`isValidRequisitionTransition`/`allRequisitionLinesPicked`/`isValidMonth`) plus the matching import additions in `server/warehouse-routes.ts` — but NO route handlers yet. Built on top of that graph rather than reinventing one (T7/T8's "read what's there first" move): added `deriveRequisitionStatusFromLines` (mirrors T6's `derivePoStatusFromLines` one level up) so `picking`/`ready` are computed server-side from the ledger's own activity (a line's real `qtyPicked`), never accepted from a client — only `approve`/`decline`/`collect` stay explicit human decisions validated against `REQUISITION_TRANSITIONS`. New routes in `server/warehouse-routes.ts`: `POST /requisitions` (submit — **`requireAuth` only**, any staff, any workspace, same universal-access spirit as the Feedback board; validates every line's `itemId` up front so a bad line never leaves an orphaned lineless requisition behind), `GET /requisitions/mine` (**`requireAuth` only** — a staff member's own submissions + lines), `GET /requisitions` + `GET /requisitions/:id` (full list/detail, `requireTab("warehouse")`, requester name joined in), `POST /requisitions/:id/{approve,decline,collect}` (`requireTab`, each gated by `isValidRequisitionTransition` — decline appends an optional reason to `notes`, collect stamps `collectedAt`), `GET /chargeback-report?month=YYYY-MM` (`requireTab`, defaults to the current NZ month via `nzTodayIso()`, sums `pick` movements by `ref_id→requisition.charge_to` priced at each item's own `cost_cents`, rounded to whole cents). Fixed two real gaps this uncovered in T8's already-committed dispatch/pick-queue code (same feature, in-scope — T8's own comment explicitly deferred requisition-status ownership to T9): (1) `POST /dispatch`'s requisition branch only accepted `status==='approved'`, which would have rejected the SECOND dispatch call the moment the first pick auto-advanced status to `picking`; (2) `GET /pick-queue` only queried `status='approved'`, so a partially-picked requisition would vanish from the queue mid-pick even with qty still remaining — both broadened to accept `approved`+`picking`. Confirmed (re-reading T8's actual code, not its Done-note prose) that `ref_id` on a requisition's pick movements is the requisition's own id, not the line id — `wh_requisition_lines.qty_picked` is a stored column written directly by dispatch, unlike a PO's derived `qty_received`, so there was never a need for `ref_id` to resolve to the line. A one-line requisition fully picked in a single dispatch call jumps straight from `approved` to `ready` (skipping a persisted `picking` moment) — deliberate: `REQUISITION_TRANSITIONS` governs discrete human-facing actions, not this evidence-derived computation, and the evidence (every line's own `qtyPicked`) is what makes the jump honest. Added 31 new assertions to `test-warehouse-shared.ts` (181 total) covering the full transition graph, `allRequisitionLinesPicked`, `deriveRequisitionStatusFromLines` (incl. the approved→ready one-shot jump and over-picked-still-counts-as-full), and `isValidMonth`. verify: `bash script/check-warehouse-gate.sh` (PASS) && `npx tsx script/test-warehouse-shared.ts` (181 passed) && `npx tsx script/test-warehouse-engine.ts` (32 passed, unaffected) && `npx tsx script/test-warehouse-scan.ts` (20 passed, unaffected) && `npm run build` (exit 0).
- [x] T10 — Equipment loans (D14, library/tool-crib model). Found this task's code almost entirely built by prior auto-checkpoint commits (`iteration 9`/`iteration 10`: `LOAN_STATUSES`/`isLoanStatus`/`CONDITION_GRADES`/`isConditionGrade`/`isLoanOverdue`/`loanLineIsReturned`/`allLoanLinesReturned`/`deriveLoanStatusFromLines`/`reasonCodeForConditionGrade` in `shared/warehouse.ts`, plus the full loans route group in `server/warehouse-routes.ts`) with a small uncommitted comment-only diff still sitting in the working tree (clarifying why check-out deliberately takes no client `idempotencyKey`) — verified line-by-line against SPEC §4.1 D14 and PLAN's own bullet rather than rebuilding (same T7/T8/T9 "read what's there first" move; PLAN.md's own Tasks-list checkbox for T10 had even already flipped to `[x]` from a checkpoint, with no matching Done-section note — this note fills that gap). `GET /loans` (status/overdue/q/borrowerContactId filters, `overdue` computed via `isLoanOverdue` against `nzTodayIso()`, never stored; per-loan line-count/qty aggregate) + `GET /loans/:id` (detail w/ lines). `POST /loans` check-out: validates every `itemId` is real AND `isLoanable` before any insert; resolves each line's real non-virtual location (explicit `locationId` or the item's own `defaultLocationId`); inserts the `wh_loans`+`wh_loan_lines` parent rows AND posts the `loan_out` movement group in ONE `db.transaction()` (via `postMovementGroup(warehouseDbFromTx(tx), ...)` directly against the shared tx, not `runMovementGroup` — the parent record and the stock movement are the same real-world event here, unlike every earlier CRUD-then-separately-movement task, so an insufficient-stock leg must roll back the loan row too); deliberately accepts no client `idempotencyKey` (a retry always makes a new loan row — same as PO/requisition create — so honouring a key on just the movement half would desync a phantom second loan from the original's movement group). `POST /loans/:id/return`: rejects a same-`loanLineId`-twice-in-one-call batch (would silently double-credit stock); rejects a line already graded in a prior call; posts ONE `runMovementGroup` `loan_return` group (one inbound leg per line, `reasonCode` derived per line via `reasonCodeForConditionGrade` from its condition grade) THEN — only on a genuinely new (non-replay) movement — stamps each line's `conditionGrade`/`conditionNote`/`replacementChargedCents` (validated non-negative-integer-or-absent via `toChargeCents`) and recomputes the loan's own `status` from every line's freshly-updated grade via `deriveLoanStatusFromLines` (never accepted from the client — same derived-status doctrine as T6/T9). Confirmed all imports wire correctly (`whLoans`/`whLoanLines`/`LOAN_STATUSES`/`isLoanStatus`/`isLoanOverdue`/`CONDITION_GRADES`/`isConditionGrade`/`deriveLoanStatusFromLines`/`reasonCodeForConditionGrade`/`type LoanStatus`/`type ConditionGrade` all present at the top of `warehouse-routes.ts`) and no test-script regressions. verify: `bash script/check-warehouse-gate.sh` (PASS) && `npx tsx script/test-warehouse-shared.ts` (191 passed) && `npx tsx script/test-warehouse-engine.ts` (32 passed, unaffected) && `npx tsx script/test-warehouse-scan.ts` (20 passed, unaffected) && `npm run build` (exit 0).
- [x] T10 — Equipment loans (D14), all in `server/warehouse-routes.ts` (schema/enums already existed from T1/T2 — no schema changes this task). `GET /loans` (status/overdue/borrowerContactId/q filters + line-count/qty aggregate) and `GET /loans/:id`, both with `overdue` computed per row via the existing `isLoanOverdue()` (never stored). `POST /loans` (check-out: borrowerName, optional borrowerContactId validated against `contacts`, dueOn, lines `[{itemId, qty, locationId?}]`) is the FIRST route in this file where a brand-new parent record and a stock movement are the SAME real-world event, so — unlike every prior task's `runMovementGroup` (self-contained transaction) — it opens its OWN `db.transaction()`, inserting `wh_loans`+`wh_loan_lines` AND calling `postMovementGroup(warehouseDbFromTx(tx), ...)` directly inside it: an insufficient-stock failure now rolls back the loan record too, instead of leaving a dangling "checked out" row with no movement behind it. This needed `notifyMovementCommitted` exported from `server/warehouse.ts` (was private) so the route can fire the post-commit sync hook itself after its own transaction commits, mirroring exactly what `runMovementGroup` does internally. Each line's checkout location is an explicit `locationId` or falls back to the item's own `defaultLocationId` (validated non-virtual via the existing `resolveRealLocationForMove`); items must have `isLoanable=true`. **Deliberately does NOT accept a client `idempotencyKey`** for check-out (unlike every other movement-posting endpoint) — since a retry always inserts a brand-new loan row regardless (same as PO/requisition create, neither of which take one either), honouring a replayed key on just the movement half would short-circuit `postMovementGroup` against a PRIOR loan's movement group while a second, phantom loan row still commits: a confusing mismatch, not real retry-safety. `POST /loans/:id/return` (body `lines: [{loanLineId, locationId, conditionGrade, conditionNote?, replacementChargedCents?}]`) posts one `loan_return` movement group (one inbound leg per line, N-leg-generalised like T6's receive) via the ordinary `runMovementGroup`, rejects a loanLineId repeated in the same call or already graded from a prior call, then — only for a genuinely new (non-replay) movement — stamps each line's condition grade/note/charge and recomputes the loan's own stored `status` via the new `deriveLoanStatusFromLines` (D14 sibling of T6/T9's per-line status derivation: LOAN_STATUSES has no partial state, so a loan stays `out` until every line is graded, then flips to `returned` + `returnedAt`, and is terminal — never re-derived once returned). A 'D' (scrap) grade tags its ledger leg with the EXISTING `write_off` reason code (`reasonCodeForConditionGrade`) — an audit tag only, not a new quarantine/routing rule; WHERE a returned item physically goes stays the operator's own `locationId` choice, same as every other leg in this file. No partial-quantity returns (a loan line returns in full, one grade — same v1 scope-cut precedent as T5's `consumeReservation`). Added to `shared/warehouse.ts`: `loanLineIsReturned`, `allLoanLinesReturned`, `deriveLoanStatusFromLines`, `reasonCodeForConditionGrade` — 10 new assertions in `test-warehouse-shared.ts` (191 total). verify: `bash script/check-warehouse-gate.sh` (PASS) && `npx tsx script/test-warehouse-shared.ts` (191 passed) && `npx tsx script/test-warehouse-engine.ts` (32 passed, unaffected) && `npx tsx script/test-warehouse-scan.ts` (20 passed, unaffected) && `npm run build` (exit 0).
- [x] T11 — Cycle counts (D12), all in `server/warehouse-routes.ts` + new pure logic in `shared/warehouse.ts`. Found a genuine partial checkpoint already sitting UNCOMMITTED in the working tree at session start (`COUNT_TRANSITIONS`/`isValidCountTransition`/`shouldHideExpectedQty` in `shared/warehouse.ts` — no routes, no test file yet) — built on top of it rather than duplicating (same T7-T10 "read what's there first" move), and specifically adopted `shouldHideExpectedQty(blind, status)` as the ONE place "hide expected from the counter" is decided, which shaped the whole route design: rather than two hardcoded views, `GET /counts/:id` (the general/counter-facing session view) calls `presentCountLines()` which strips `expectedQty`/`varianceQty`/`needsRecount`/`resolution` via that same function (blind sessions stay hidden through `open` AND `submitted`, only becoming transparent once `approved` — D12's "counter never sees expected" is about the whole in-progress lifecycle, not just data-entry), while a SEPARATE `GET /counts/:id/variance` endpoint is always-full regardless of blind/status — PLAN's own named "variance list" deliverable, for an approver deciding whether to approve. `POST /counts` auto-populates `wh_count_lines` from CURRENT `wh_stock` filtered by the given scope (`scopeZone` -> `wh_locations.zone`, `scopeClass` -> `wh_items.category`, always excluding `kind='virtual'` locations — nobody counts a bin with no printed label) — validated non-empty BEFORE any insert so a scope matching nothing never creates an orphaned, lineless session; `expectedQty` is a pure snapshot of `on_hand` at creation time, never recomputed later even if other movements land in the same bins mid-count. `POST /counts/:id/count` records one or more lines' `countedQty` (rejects a same-lineId-twice-in-one-call batch, same guard style as T10's loan return), computing `resolution` via the new `countLineResolution()` — allowed while `open` OR `submitted` (not just `open`) so a flagged `recount` line can be re-entered in place without reopening the whole session, blocked only once `approved`. `POST /counts/:id/submit` (`open`->`submitted` via `isValidCountTransition`) requires EVERY line already counted — the one completeness gate that makes `POST /counts/:id/approve`'s later work trustworthy. Approve: `canApproveCount()` folds the status-chain check + D12's "counter ≠ approver" guard (both columns are the same `users` FK — enforced app-side, not a DB CHECK, same reasoning as everywhere else this schema can't self-reference-compare) into one call; the new `buildCountAdjustmentLegs()` turns every line whose count actually differs from its snapshot into a `LocationMoveLeg` (reusing T7's existing interface verbatim rather than inventing a near-duplicate — no per-leg reasonCode needed since the whole group shares one, `count_variance`), skipping exact matches (nothing to correct) and any never-counted line (defensive; shouldn't be reachable post-submit). Movement type is `adjustment` (not `count` — D12's own wording is "posts **an adjustment movement**"; `count` the MovementType is left for a *future* pure count-only ledger event this task doesn't need). Like T10's loan check-out, the movement post and the session's status flip to `approved` share ONE `db.transaction()` (`postMovementGroup(warehouseDbFromTx(tx), ...)` called directly, not `runMovementGroup`) rather than two separate steps — because approve takes NO client `idempotencyKey` (same reasoning as requisition approve/decline/collect: the status-guard alone is what makes a retry a clean 409), a movement that committed but whose status-flip failed would leave `canApproveCount` still passing on retry and re-post the same adjustments a second time; one transaction makes that impossible. A perfectly-matching count (zero variance everywhere) posts no movement group at all — `legs.length > 0` gates the call, proven in the test file by asserting nothing was recorded. New `script/test-warehouse-counts.ts` (29 assertions): the full variance-math table (exact match / under-threshold / over-10%-relative / exactly-at-threshold-not-over / over-$100-absolute / missing-cost-only-percentage-applies / zero-expected-divide-guard), the status chain + blind-visibility matrix, the approval guard (different approver ok / same approver blocked / null countedBy ok / not-yet-submitted blocked / already-approved blocked), `buildCountAdjustmentLegs` edge cases (skip-match, skip-uncounted, sign correctness, allowNegative threading, mixed batch ordering, empty input), and 4 end-to-end proofs through the real `postMovementGroup` (mixed-variance group posts correctly with the right `movementType`/`reasonCode`/`ref`, a perfect count posts nothing, a downward correction exceeding on-hand rejects the WHOLE approval, `allow_negative` lets it through anyway). verify: `bash script/check-warehouse-gate.sh` (PASS) && `npx tsx script/test-warehouse-counts.ts` (29 passed) && `npx tsx script/test-warehouse-shared.ts` (191 passed, unaffected) && `npx tsx script/test-warehouse-engine.ts` (32 passed, unaffected) && `npx tsx script/test-warehouse-scan.ts` (20 passed, unaffected) && `npm run build` (exit 0).
- [x] T12 — Channel sync, `server/warehouse-sync.ts` (new file) per SPEC §4.3, plus a small schema extension it genuinely needed. **Found this session shared the worktree with a second, concurrent agent run** (see the AGENTS.md lesson below) — its auto-checkpoint had already landed identical `wh_shopify_variant_links`/bigint-`ref_id` schema changes (byte-for-byte the same as this run's own, both reasoning from the same SPEC/AGENTS constraints) plus a stray `BLOCKED.md` hand-off note and an unused `findReservationForRef` export in `server/warehouse.ts` from its own in-progress `server/warehouse-sync.ts` draft. Kept the (identical) schema work, deleted `BLOCKED.md` (its "mid-flight" state no longer applies — T12 is finished and verified) and removed the orphaned `findReservationForRef` (this run's own partial-refund-release logic composes `reservationDbFromTx`+`releaseReservation` instead, needing no new export on `server/warehouse.ts`), restoring that file to its exact T11 content.
  - **Schema (additive):** new `wh_shopify_variant_links` table (item↔extra-sibling-variant links for the SIU Plain/Player/Custom fan-out — `whItems`' own `shopify_variant_id`/`shopify_inventory_item_id` stay the PRIMARY mapping, untouched, so T4/T8's existing code needs zero changes). **`whMovements.refId`/`whReservations.refId` widened `integer`→`bigint({mode:'number'})`** — a real bug this task would otherwise have shipped: a Shopify order's numeric id routinely exceeds Postgres `int4`'s ~2.1bn ceiling, and `mode:'number'` keeps the TS type identical to plain `integer` (JS numbers are exact to 2^53), so every already-committed T3/T5/T6/T8/T9/T10/T11 caller needed zero changes. `script/apply-warehouse.ts`'s verification arrays updated to match (cross-checked table-for-table and index-for-index against the migration file with a throwaway node script — exact match, no drift).
  - **The engine (`server/warehouse-sync.ts`):** a minimal fetch-based Shopify GraphQL client (`inventorySetQuantities` push, an `inventoryLevel` query for reconcile, a `fulfillmentCreate` stub for a future task to wire into T8's own stub — all three flagged `// TODO-verify(live)`, written from SPEC prose, never run against a real store); a generic `Debouncer<K>` (~5s, injectable timer fns so it's provably correct with zero real waiting) wired into `server/warehouse.ts`'s existing `onMovementCommitted` hook; `isEchoOfOurPush`/`computeDrift` (D9's echo-suppression-then-drift-detection two-step); `fanOutTargets` (primary mapping + same-store sibling links, de-duped, cross-store sibling rows skipped as a data-error guard); `pushItemUsing` (computes `available` once, pushes to native `shop_variants.stock` AND every fanned-out Shopify variant, one `wh_sync_state` row per item×store regardless of how many physical Shopify variants received it); webhook processing (`processShopifyOrderCreate`/`Cancelled`/`RefundCreate`/`processInventoryLevelsUpdate`) behind one dedupe-then-dispatch entry point (`handleShopifyWebhookEvent`) that the actual Express route (`registerWarehouseSyncRoutes`, HMAC-verified via `req.rawBody` — already captured globally by `server/index.ts`'s `express.json({verify})`, same pattern as the existing CUGC Stripe webhook) calls after checking the signature; `reconcilePollUsing`/`runReconcilePoll` (D9's 10-min poll — exported for a future cron to call, no scheduler wired here, matching PLAN's own "a reconcile poll function" wording). Every DB-touching piece takes a bespoke `SyncDb` seam (same reasoning as T3/T5/T7's `WarehouseDb`/`ReservationDb`/`ScanLookupDb`) and a `ReservationOps` seam (so webhook processing is unit-testable via AGENTS.md's "fake tx/db object that records calls" without ever reaching `runReserveStock`'s lazy `db` import); the real adapters (`syncDbFromDb`, `realReservationOps`) are never unit-tested directly.
  - **A correctness fix the sibling fan-out makes newly necessary:** `reserveStock` (T5) is idempotent per (ref,item) — a SECOND call for an item already reserved against an order is a silent no-op, by design. Once one order can legitimately contain two DIFFERENT Shopify variants (e.g. a Plain shirt AND a Custom-printed one, same size) that both map to the SAME `wh_item`, calling `reserveStock` once per LINE would silently drop every line after the first that shares an item. `processShopifyOrderCreate` aggregates order lines to `wh_item` and SUMS their quantity BEFORE calling `ops.reserve` — one call per unique item, correct total — covered explicitly in the test file.
  - **Partial refunds release per-item, not per-order:** `orders/cancelled` legitimately releases every reservation on the order (`releaseAllForRef`), but a `refunds/create` can cover only SOME lines — added `releaseForRefItem` (real impl composes the EXISTING `reservationDbFromTx(tx).findActiveReservationByRef` + `releaseReservation`, no new export needed anywhere) so only the actually-refunded (non-`no_restock`) items release.
  - **Env-gated, inert without config** (AGENTS.md): `WH_SYNC_ENABLED` gates the debounced push firing at all; per-store `WH_SHOPIFY_<STORE>_DOMAIN`/`_TOKEN`/`_LOCATION_GID`/`_WEBHOOK_SECRET` are read lazily per call, never cached; a webhook for a store missing its secret 200s an ignored event (no HMAC secret to crash on, and no reason to make Shopify hammer retries against an unconfigured store).
  - New `SHOPIFY_STORES`/`isShopifyStoreKey` + `SYNC_STORES`/`isSyncStore` added to `shared/warehouse.ts` (the enum-ish `store` column, house rule — validated app-side, no DB CHECK) since a future T16c sync dashboard will want the same validator client-side; the echo/drift/debounce/fan-out MATH itself stays server-only in `warehouse-sync.ts` (nothing a client would ever import), mirroring the shared-vs-server split this spec already draws for the engine. 7 new assertions in `test-warehouse-shared.ts` (213 total).
  - New `script/test-warehouse-sync.ts` (51 assertions): env-config inertness, HMAC verify (correct/tampered/wrong-secret/missing/mismatched-length), `toSafeRefId` (safe-integer pass, unsafe/NaN throws), debounce collapse + independent-key tracking (fake timer fns, zero real waiting), echo suppression (in/out of window, qty mismatch, no-baseline), drift detection (echo/no-baseline/match/genuine-mismatch), fan-out (dedup, cross-store skip), `pushInventoryToShopify` request shape + userErrors/non-ok-HTTP handling (mocked fetch), `pushItemUsing` end-to-end (4-target fan-out, native-only skips fetch entirely, negative-available clamps to 0 for native, missing store config skips cleanly, unknown item no-ops), order-create sibling aggregation (the one-call-not-two fix, unmapped/zero-qty lines skipped), order-cancelled full release, refund partial release + no_restock + same-item dedup, `processInventoryLevelsUpdate` echo/drift/unmapped, `handleShopifyWebhookEvent` dedupe (same webhook id redelivered does NOT double-reserve, a different id for the same order IS processed) + full topic dispatch, `reconcilePollUsing` (heals+alerts on mismatch, no-op on agreement, native-only and unconfigured-store items skipped).
  - verify: `bash script/check-warehouse-gate.sh` (PASS) && `npx tsx script/test-warehouse-sync.ts` (51 passed) && `npx tsx script/test-warehouse-shared.ts` (213 passed) && `npx tsx script/test-warehouse-engine.ts` (32 passed, unaffected) && `npx tsx script/test-warehouse-scan.ts` (20 passed, unaffected) && `npx tsx script/test-warehouse-counts.ts` (29 passed, unaffected) && `npm run build` (exit 0).
- [x] T13 — Native-cart integration, the ONE flagged edit into another feature's file (AGENTS.md's exception). Added a single `if (process.env.WMS_NATIVE_SYNC === "1")` block inside `finalizeShopOrderPaid`'s existing per-item stock-down loop in `server/shop-routes.ts` (the shared webhook+client-confirm idempotent finalize, per D11): for each `item.variantId`, looks up a `wh_items` row mapped to it (`shopVariantId`) and — only if mapped — calls `runReserveStock({itemId, qty: item.qty, ref: {kind:'shop_order', id: order.id}})`. Entirely additive and best-effort (its own try/catch, never throws into the paid-order flow) — the existing `shop_variants.stock` decrement immediately above stays untouched as the checkout availability backstop, exactly as D11/PLAN specify ("checkout availability reads pushed `shop_variants.stock`, unchanged contract"). Deliberately scoped to `finalizeShopOrderPaid` only (the single-order payment-confirm path PLAN.md names) and NOT `finalizeTeamOrderAllPaid` (the Player-Pay/team-kit path, which decrements by roster-size-per-colour rather than a simple `item.variantId`) — extending team orders would need a colour+size→variant→wh_item resolution chain that isn't "smallest possible diff" and PLAN's own T13 line only ever says "the payment-confirm path" (singular); a future task can extend it if Daniel wants team-order reservations too. `qty` needed no cents/numeric(12,3) conversion — `shop_order_items.qty` is already a plain integer column, and `ReserveInput.qty: number` takes it as-is. No new tables/tests — this is orchestration glue between two already-tested systems (T5's `reserveStock`, the existing shop finalize), so no new `wh_*` logic was introduced to unit-test; verified instead by confirming the diff is genuinely minimal and every existing warehouse test suite is unaffected. Reservation failures (e.g. item not found, insufficient availability) are logged and swallowed exactly like the pre-existing stock-decrement's own try/catch one line above it — a WMS hiccup must never fail a customer's already-successful Stripe payment. Dark-launched: `WMS_NATIVE_SYNC` is unset in every environment today, so this block never executes until a human sets it. verify: `bash script/check-warehouse-gate.sh` (PASS, zero new warehouse-file errors) && `git diff --stat server/shop-routes.ts` (1 file changed, 19 insertions(+), 0 deletions — imports + the one new conditional block, no existing line altered) && `npm run build` (exit 0) && full regression across every existing test script, all unaffected: `test-warehouse-shared.ts` (213 passed), `test-warehouse-engine.ts` (32 passed), `test-warehouse-scan.ts` (20 passed), `test-warehouse-counts.ts` (29 passed), `test-warehouse-sync.ts` (51 passed).
- [x] T15 — Scan station, `client/src/pages/warehouse-scan.tsx` (SPEC §4.4/§4.5). Single-file page: a `useCamera` hook runs ONE getUserMedia stream + a 300ms detection-loop interval, shared by both the idle scan loop and any mid-form "scan a second code" sub-flow (`picking` state) — starting a fresh stream per use would be slow/flickery on mobile. Detector: native `window.BarcodeDetector` feature-detected via its own `getSupportedFormats()` CONTENTS (never `'BarcodeDetector' in window`, per AGENTS.md) filtered against a `WANTED_FORMATS` list (qr_code/code_128/code_39/ean_13/ean_8/upc_a/upc_e/itf/codabar); falls back to the `barcode-detector` npm ponyfill (added `barcode-detector@3.2.1`, exact-pinned) via a lazy `import("barcode-detector/pure")` (confirmed Vite code-splits this into its own chunk, not the main bundle) with `setZXingModuleOverrides({locateFile})` pointed at a SELF-HOSTED wasm — copied `node_modules/zxing-wasm/dist/reader/zxing_reader.wasm` (zxing-wasm is barcode-detector's own transitive dep, hoisted to top-level `node_modules`) into `client/public/zxing/zxing_reader.wasm`, served at `/zxing/zxing_reader.wasm` by Vite's default `publicDir`, rather than the package's default jsDelivr CDN. Torch: `track.getCapabilities().torch` + `applyConstraints({advanced:[{torch}]})`, button only rendered when supported. Feedback: a WebAudio oscillator beep (880Hz ok / 200Hz err) + `navigator.vibrate` (short buzz ok / triple buzz err) on every resolve and every sub-action success/failure.
  - **Resolution + action sheet:** `POST /api/admin/warehouse/scan` (T7) drives everything; a local `ScanResolution` type mirrors the server's (client can't import `server/*`). `actionsFor()` combines the server's advisory `resolution.actions` (`MovementType[]`) with a client-added "receipt" (Receive) for item-resolved codes only — T7's `scanActionsForItem`/`scanActionsForLocation` never included "receipt" in their lists (receiving is PO+item driven, not itself one of the MovementTypes those helpers return), so it's prepended here rather than expecting the shared helper to grow a case it was never designed for. Re-scanning the SAME raw code while still on the action sheet (before opening a specific action form) increments a `scanCount` instead of re-hitting the resolve endpoint — for an alias match (`packQty > 1`, e.g. a case-of-6 barcode) this feeds `scanQuantityToUnits(scanCount, packQty)` as every form's default qty, so scanning a case barcode 4 times defaults to 24 eaches, matching AGENTS.md's own worked example.
  - **New backend endpoint this task genuinely needed:** `POST /api/admin/warehouse/consume` in `server/warehouse-routes.ts` (body `{itemId, locationId, qty, printOrderId, reasonCode?, note?, idempotencyKey?}`) — SPEC/PLAN's own action list names "Consume→print job" but no T4-T11 endpoint ever posted the `consume` MovementType (confirmed by grep — `"consume"` only existed in the taxonomy arrays, never in an actual route). One outbound leg, no incoming leg (same "stock leaves the building" shape as T8's requisition-pick/shop-order-dispatch), `ref: {kind:'print_order', id: printOrderId}` (validated against the EXISTING `print_orders` table — added a read-only `printOrders` import to `@shared/schema`, no prints-feature file touched). `reasonCode` is optional and, if given, validated against the existing D15 `REASON_CODES` — no new taxonomy invented, same discipline as T6's damaged-only-quarantine call. This stayed entirely inside `server/warehouse-routes.ts` (the warehouse feature's own file), so it doesn't violate AGENTS.md's "don't touch other features' code" rule.
  - **Every other action reuses an EXISTING endpoint verbatim:** Putaway/Transfer → `POST /putaway`|`/transfer` (T7). Receive → search+pick an open PO (`GET /pos` filtered client-side to `sent|partial|received`, matching `RECEIVABLE_PO_STATUSES`) → fetch its detail (`GET /pos/:id`) → auto-match the line by `itemId` (a PO missing a line for the scanned item shows an amber warning rather than guessing) → `POST /pos/:poId/receive` (T6). Pick/Dispatch → ONE shared `PickDispatchForm` parameterised by `restrict: "pick"|"dispatch"`, fetches `GET /pick-queue` (T8/T9) and filters to `requisitions` (Pick) or `nativeOrders`+`shopifyOrders` (Dispatch) containing the scanned item, qty capped at the queue entry's own remaining/qty, posts via `POST /dispatch`. Loan out → `POST /loans` (T10) with borrower name/due date/qty/bin. Return → search `GET /loans?status=out`, pick a loan, its detail's ungraded lines pre-select the one matching the scanned item if present, condition grade + optional note + return bin → `POST /loans/:id/return`. Count → search `GET /counts?status=open`, pick a session, filter its lines to `locationId === scanned location` AND not yet counted, enter each → `POST /counts/:id/count`.
  - **Verification:** confirmed the module genuinely bundles under Vite (not just tsc) by TEMPORARILY wiring an import+route into `App.tsx`, running `npm run build` (module count 3730→3734, a real `ponyfill-*.js` chunk appeared — proving the dynamic `barcode-detector/pure` import resolves and code-splits correctly), then `git checkout -- client/src/App.tsx` to revert — actual route/nav wiring stays T17's job per AGENTS.md's own checklist, same task-boundary discipline as T14 leaving `warehouse-labels.tsx` unwired. verify: `bash script/check-warehouse-gate.sh` (PASS) && `npm run build` (exit 0, clean, unwired-page state) && full regression unaffected: `test-warehouse-shared.ts` (213), `test-warehouse-engine.ts` (32), `test-warehouse-scan.ts` (20), `test-warehouse-counts.ts` (29), `test-warehouse-sync.ts` (51).

- [x] T14 — Label printing, `client/src/pages/warehouse-labels.tsx` (D5/§4.4). Found this task essentially fully built already, uncommitted on disk (untracked file, plus the matching `qrcode`/`@types/qrcode` deps added uncommitted to `package.json`/`package-lock.json`) — a crashed prior run's work, same "read what's there first" move as T7-T11. Verified it line-by-line against SPEC §4.4/D5 and PLAN's own bullet rather than rewriting: one page, a `kind` toggle (item ↔ location) + `layout` toggle (single 50×25mm ↔ A4 grid), a search-and-checkbox picker sourced from the existing T4 `GET /api/admin/warehouse/items|locations` list endpoints (client-side filter, same convention as `prints-materials.tsx`), QR rendered CLIENT-SIDE via `QRCode.toString(value, {type:"svg"})` from the `qrcode` npm pkg (never a server-generated image — the server's T4 label-payload endpoints only ever hand back the payload STRING, confirmed unused by this page since the already-fetched item/location rows already carry everything needed: `sku` verbatim for items, `locationBarcodePayload(code)` — the same `LOC:` helper T7's scan resolver decodes with — for locations) with a module-level cache so re-selecting a previously-rendered SKU/code never re-renders its QR twice. `?kind=item|location&ids=1,2,3` read on mount (via wouter's `useSearch()`) pre-populates the selection for a future T16a "Print label(s)" deep link, but the page works fully standalone with none of that (T16a/T17 don't exist yet — this task only builds the page, not its nav wiring, per AGENTS.md's own task boundary). **Found and fixed two real gaps while verifying:** (1) the print stylesheet's `@media print { .wh-print-hide { display: none !important; } ...}` rule had gone missing from the on-disk copy by the time of a second `Read` mid-task (content differed from the first `Read` at the top of this run, despite `git status`/`git log` showing no new commit or stash the whole time — logged as a new, narrower variant of the T7-T12 "don't trust one snapshot of a file's contents for a whole task's duration" lesson below) — without it, printing would have printed the entire admin chrome (sidebar/header/picker) alongside the labels; restored it. (2) the on-screen "Preview — what will print" box rendered a placeholder only when zero items were selected and literally nothing when items WERE selected (the real mm-sized label boxes rendered as an unstyled, unheaded section further down the page instead) — re-plumbed so the live `.wh-print-area` renders NESTED inside the bordered preview box (a `position:fixed` print-area child can still escape an ancestor styled only with `visibility:hidden`, since — unlike `display:none` — `visibility` can be overridden by a descendant; it just can't live inside `.wh-print-hide` itself, whose `display:none` unconditionally hides descendants). Also added `break-inside: avoid` to the base `.wh-label` card (not just the single-layout wrapper) so a grid-layout row landing on a page boundary can't split an individual label's QR/text across two physical sheets. No route/nav wiring added (T17's job) — confirmed via an unchanged Vite module count (3730, before and after this task's edits) that the page isn't yet reachable from the running app, exactly as expected at this point in the plan. verify: `bash script/check-warehouse-gate.sh` (PASS) && `npm run build` (exit 0, clean) — `npx tsx script/test-warehouse-*.ts` suites unaffected (no shared/server files touched this task).
- [x] T16a — Admin pages 1. Found no prior work (unlike T7-T14's repeated "crashed run left a draft" pattern) — clean build from PLAN's own bullet. **Backend additions this task genuinely needed** (none of T4-T15's routes computed any of this): `GET /api/admin/warehouse/dashboard` (one aggregated read — low-stock items via a raw-SQL `SUM(on_hand) FILTER (WHERE kind<>'virtual')` vs `min_qty` per active item, same "computed aggregate isn't expressible via the query builder" reasoning as T6's `getPoLineReceiptTotals`; pending requisitions via the existing `REQUISITION_STATUSES` minus collected/declined; overdue loans via `status='out' AND due_on < today`; drift alerts via `wh_sync_state` rows with a `lastDriftAt`; recent movements = the ledger's own tail) + `GET /api/admin/warehouse/items/:id/stock` (per-location on-hand) + `GET /api/admin/warehouse/items/:id/movements` (per-item ledger, newest first, `?limit=` capped at 500) — all three added to `server/warehouse-routes.ts` (the warehouse's own file, so within AGENTS.md's "don't touch other features' code" bound) and reuse existing nested helpers (`fullName`, `withOverdue`) via function-declaration hoisting rather than duplicating them. `client/src/pages/warehouse-dashboard.tsx` (stat cards + 4 alert panels + recent-movements feed, styled after `prints-dashboard.tsx`), `client/src/pages/warehouse-items.tsx` (search/kind/brandOwner/active filters, a dense list with bulk-select checkboxes, a detail modal combining the item record + per-location stock + last-50 movements + aliases + inline edit/delete, and a create-item modal), `client/src/pages/warehouse-locations.tsx` (same list+bulk-select shape, simpler CRUD modal — `zone` is deliberately NOT a form field since T4's `deriveLocationZone` computes it server-side, shown read-only instead). Both admin pages' bulk-select "Print labels" button deep-links to `warehouse-labels.tsx` (T14) via its own documented `?kind=item|location&ids=1,2,3` contract — the first task to actually exercise that deep link. Reused `shared/warehouse.ts`'s existing label dictionaries (`ITEM_KIND_LABELS`, `BRAND_OWNER_LABELS`, `UNIT_LABELS`, `MOVEMENT_TYPE_LABELS`, `REASON_CODE_LABELS`) rather than redefining them client-side. **Route-path convention chosen for T17** (no routes wired yet — that's still T17's job per AGENTS.md's checklist, and these pages are unreachable from the running app today, confirmed via the same "unchanged Vite module count until temporarily wired" check T14/T15 used): dashboard at the tab's own `/admin/warehouse`, sub-pages nested `/admin/warehouse/{items,locations,pos,requisitions,loans,counts,sync,ledger,scan,labels}` — every internal link in these 3 pages (and the existing scan/labels pages) already points at these exact paths, so T17 must wire to them verbatim, not invent different ones. verify: `bash script/check-warehouse-gate.sh` (PASS) && `npm run build` (exit 0; confirmed all 3 pages actually bundle by temporarily wiring imports+routes into `App.tsx`, rebuilding — module count 3730→3734 — then `git checkout -- client/src/App.tsx` to revert, same T14/T15 move) — full regression unaffected: `test-warehouse-shared.ts` (213), `test-warehouse-engine.ts` (32), `test-warehouse-scan.ts` (20), `test-warehouse-counts.ts` (29), `test-warehouse-sync.ts` (51).
  - **Verify-and-finish pass (separate run):** this task landed via the T12/T15-style live concurrent collision — found `server/warehouse-routes.ts` mid-edit and the 3 pages appearing one-by-one in real time, waited for `git status`/`git diff` to stabilise, then it self-committed as `d6e4fe7` before this run wrote anything. Verified the whole diff line-by-line against SPEC §4.4/PLAN's own bullet and every schema column referenced (`whRequisitions.chargeTo`/`whLoans.dueOn`/`whSyncState.lastDriftAt` etc. all real, `nzTodayIso()` used correctly for the overdue-loan date compare, no DB CHECKs, read-only endpoints never touch `wh_stock`/`wh_movements`) rather than trusting it. **Found and fixed two real bugs while verifying, same "verify-and-finish can still find real gaps" precedent as T14:** (1) `warehouse-items.tsx`'s `<ItemDetailModal>` had no `key` prop, so its internal `editing`/`form` state persisted across different items — mid-edit item A, close without saving, open item B, and B's detail would render with A's stale unsaved form values still active in edit mode, risking a Save silently writing A's field values onto B's row; fixed with `key={detailId ?? "none"}` (the exact pattern the sibling Locations page's own `EditModal` and `prints-materials.tsx` already use for the identical reason). (2) `warehouse-locations.tsx`'s "New location" `EditModal` instance (`location={null}`, no key, so never remounts) never reset its `code`/`kind`/`active` fields after a successful create, so a second "New location" click showed the just-created values instead of a blank form; fixed by resetting those 3 fields in `save`'s `onSuccess` when `isNew` (mirrors `warehouse-items.tsx`'s own `CreateItemModal` resetting via `setForm(EMPTY_FORM)` on success). Both are UI-only fixes, no server/shared changes, so the full regression suite is unaffected by construction — re-ran it anyway. verify (re-run fresh after the fixes): `bash script/check-warehouse-gate.sh` (PASS) && `npm run build` (exit 0, module count unchanged/unwired) && `test-warehouse-shared.ts` (213), `test-warehouse-engine.ts` (32), `test-warehouse-scan.ts` (20), `test-warehouse-counts.ts` (29), `test-warehouse-sync.ts` (51) — all unaffected.
- [x] T16b — Admin pages 2, all built entirely from existing T4-T10 endpoints — no backend changes needed this task (unlike T16a, which had to add 3 read endpoints first). `client/src/pages/warehouse-pos.tsx`: list+filter+create over `wh_purchase_orders`, a detail modal with free-form status PATCH (POs have no transition graph, T6's own comment — any status is directly settable, e.g. draft→sent to unlock receiving) + inline per-line "Receive" form (qtyGood/qtyDamaged/location/note → `POST /pos/:poId/receive`, success toast reuses `receiveDiscrepancyNote()` from `shared/warehouse.ts` verbatim rather than re-deriving the over/short/damaged message client-side) + an add-line form + delete guards that surface the server's 409s as toasts (line/PO deletion blocked once anything's been received — enforced server-side, this page just relays the message). A local `RECEIVABLE_STATUSES` set mirrors the module-private `RECEIVABLE_PO_STATUSES` in `server/warehouse-routes.ts` (not exported — server-only by design) purely to hide the Receive button pre-emptively; the server stays the real gate. `client/src/pages/warehouse-requisitions.tsx`: a two-mode page (Queue / Chargeback report, plain button toggle rather than the unused `Tabs` UI component — matches T16a's "no new component dependencies" precedent) — Queue lists+filters+creates requisitions (submit via the same `requireAuth`-only `POST /requisitions` any staff can call, just reached through this tab-gated admin page rather than a separate staff-facing UI, which PLAN/SPEC never asked for as a T16 deliverable) and its detail modal renders approve/decline/collect actions gated by `status` (mirroring `REQUISITION_TRANSITIONS` — submitted→approve/decline, ready→collect) plus, for `approved`/`picking` requisitions, a per-line "Pick" control that calls `POST /dispatch` with `sourceKind:'requisition'` (T8) — status auto-advances server-side (`deriveRequisitionStatusFromLines`), never set by this page directly. Chargeback report is a `type="month"` picker against `GET /chargeback-report`. `client/src/pages/warehouse-loans.tsx`: list+filter (status/overdue/borrower search) + a check-out modal (borrower/dueOn/notes/lines, item picker pre-filtered to `isLoanable` items, defaults each line's location to the item's own `defaultLocationId` and warns when one's missing since check-out has no location fallback) + a detail modal whose per-line return control POSTs `/loans/:id/return` ONE LINE AT A TIME (`lines:[{loanLineId,...}]` with a single entry) rather than batching a multi-select — simpler to reason about, and the endpoint is equally correct either way since D14 has no partial-quantity-return state to worry about splitting across calls. All three pages reuse the exact `ItemPicker` shape (type-to-filter over the already-fetched active items list, same "fetch once, filter client-side" call T16a made for locations) independently per file rather than factoring it into a shared component — three ~30-line near-identical copies, each with a task-specific filter (loans' pre-filters to loanable items) — matches the "three similar lines beats a premature abstraction" guidance and T16a's own per-file `Form`/`EditModal` precedent of not sharing UI code across these pages. `CONDITION_GRADE_LABELS`/`PO_STATUS_LABELS`/`REQUISITION_STATUS_LABELS` reused from `shared/warehouse.ts`; `LOAN_STATUS_LABEL` defined locally (no shared export exists for it, same as T16a's local `KIND_LABEL` for `LOCATION_KINDS`). **Applied T16a's own verify-pass lesson pre-emptively**: all 3 detail modals (`PoDetailModal`/`RequisitionDetailModal`/`LoanDetailModal`) are rendered with `key={detailId ?? "none"}` at their call sites — without it, `PoDetailModal`'s `editing` form state (and `RequisitionDetailModal`'s `declining`/`declineReason`) would survive closing one record and opening a different one, same stale-data-corruption class T16a found and fixed in `warehouse-items.tsx`/`warehouse-locations.tsx`. verify: `bash script/check-warehouse-gate.sh` (PASS) && `npm run build` (exit 0; same temporarily-wire-into-`App.tsx`-then-revert check as T16a — module count rose 3730→3734 with all 3 new pages wired, confirming they bundle; `git checkout -- client/src/App.tsx` reverted cleanly, confirmed via `git status --short`) — full regression suite unaffected: `test-warehouse-shared.ts` (213), `test-warehouse-engine.ts` (32), `test-warehouse-scan.ts` (20), `test-warehouse-counts.ts` (29), `test-warehouse-sync.ts` (51).
  - **Verify-and-finish pass (separate run):** this task landed via yet another live concurrent collision (fourth occurrence of the T12/T15/T16a pattern) — polled `git status --short` every ~15s from a clean T16a HEAD, watched `warehouse-pos.tsx` → `warehouse-requisitions.tsx` → `warehouse-loans.tsx` appear one at a time over ~13 minutes (with a brief `App.tsx` diff for the bundle-check, then a `PLAN.md` diff), then it self-committed as `92ba435` before this run wrote a single line of its own code. Independently re-verified every claim rather than trusting the Done note: cross-checked all three pages' request/response bodies against the actual `server/warehouse-routes.ts` handlers (PO create/patch/lines/receive incl. the `discrepancy` response field consumed via `receiveDiscrepancyNote()`; requisition submit/list/detail/approve/decline/collect incl. the decline `{reason}` body and the `POST /dispatch` `{sourceKind:'requisition', sourceId, itemId, locationId, qty}` shape; loan check-out/return) and every schema field referenced (`WhItem.defaultLocationId`/`isLoanable`/`costCents`, `WhLoan.borrowerName`/`dueOn`, `WhRequisition.chargeTo`/`neededBy`, `WhPurchaseOrder.supplierName`/`expectedOn` all real) — no drift found. Confirmed all 3 detail modals already carry `key={detailId ?? "none"}` (T16a's own lesson applied pre-emptively, as the note claims). **Found one pre-existing documentation defect while verifying** (not a code bug): T16a's own "Verify-and-finish pass" sub-bullet — about `warehouse-items.tsx`/`warehouse-locations.tsx`, nothing to do with T16b — had been pushed to sit visually *after* the newly-inserted T16b bullet by this commit, making it read as if it were T16b's verify note; reordered so T16a's bullet and its own sub-bullet stay adjacent and T16b's (new) sub-bullet follows T16b's own entry. General lesson for any future task editing this Done section: a `- [x] T<n> — ...` bullet inserted via `Edit`'s string-replace can land *before* an existing indented sub-bullet that actually belongs to the *previous* entry if the anchor text used for the insertion point doesn't include that sub-bullet — check what immediately follows the insertion point, not just what precedes it. verify (re-run fresh, nothing to fix in code): `bash script/check-warehouse-gate.sh` (PASS) && `npm run build` (exit 0, module count unchanged/unwired, confirmed via `git status --short` after `git checkout -- client/src/App.tsx`) && `test-warehouse-shared.ts` (213), `test-warehouse-engine.ts` (32), `test-warehouse-scan.ts` (20), `test-warehouse-counts.ts` (29), `test-warehouse-sync.ts` (51) — all unaffected.
- [x] T16c — Admin pages 3. Found no prior work (clean build from PLAN's own bullet, unlike T7-T16b's repeated "crashed/concurrent run left a draft" pattern). **Backend additions this task genuinely needed** in `server/warehouse-routes.ts` (all three read from `wh_*` tables no prior route ever exposed at this grain): `GET /movements` — the general cross-item ledger `GET /items/:id/movements`'s own comment already named as "T16c's job", filterable by itemId/locationId/movementType/reasonCode/refKind/q (ilike over sku/name/location code)/dateFrom/dateTo (both validated `isValidDateOnly`, applied as `createdAt::date` bounds — never round-tripped through a JS `Date`, house rule) capped at 500. `GET /sync-state` — one aggregated read (T16a's "dashboard" precedent) merging every channel-mapped item (native `shopVariantId` and/or Shopify `shopifyStore`+`shopifyVariantId`) with its `wh_sync_state` row per (item, store), read-only `wh_shopify_variant_links` (sibling mappings) joined to sku/name, and a `driftLog` (rows with `lastDriftAt` set, sorted newest-first) — plus `enabled`/per-store `configured` flags from T12's `isSyncEnabled()`/`getShopifyPushConfig()`. **Applied T12's own forward-looking comment verbatim**: `pendingPush` is computed from `getPendingPushItemIds()` (the in-process debounce queue) rather than reading `wh_sync_state.pending`, which that table's own schema comment says is deliberately always written `false`. `POST /sync-state/:itemId/push` and `POST /reconcile-poll` — thin wrappers calling T12's already-built `pushItemNow()`/`runReconcilePoll()` (both existed, unused by any route until now); importing them at the top of `warehouse-routes.ts` needed none of T3/T12's lazy-`db`-import ceremony since this file is never loaded by a DB-free `tsx script/test-warehouse-*.ts` run (T13's own lesson, confirmed by grep before adding the import).
  - `client/src/pages/warehouse-counts.tsx`: create-session modal (optional scopeZone/scopeClass text — blank means "count everything", blind checkbox default true) → list+filter → detail modal whose body is entirely status-driven: while `open`/`submitted` it renders whatever `GET /counts/:id` already gave back (blind-stripped or full — the SERVER decides via `shouldHideExpectedQty`, this page never re-implements that check) as a per-line qty-entry form; `submitted` additionally fetches the ALWAYS-full `GET /counts/:id/variance` for the approver's review, showing only genuinely-varying lines with a "Recount" affordance that re-POSTs `/count` for one flagged line (no status transition needed — allowed while still `submitted`, per T11's own route) before Approve; `approved` just shows the timestamp. Never invents a "counter vs approver" UI distinction — the server's `canApproveCount` (counter ≠ approver, D12) is the only enforcement, surfaced here purely as a 409 toast if violated.
  - `client/src/pages/warehouse-sync.tsx`: a status banner (push-queue enabled/disabled + per-store configured/not) + "Run reconcile poll now" button, then two tabs — Mappings & push state (the merged `rows` table + sibling-links sub-table, each row with a "Push now" button) and Drift log (`driftLog`, same push button — D9's "WMS is master" doctrine means healing a drift always means RE-asserting our own number, never adopting Shopify's, so the identical push action is correct in both places).
  - `client/src/pages/warehouse-ledger.tsx`: read-only filterable feed (q/location/movementType/reasonCode/refKind/date range) over the new `GET /movements`, styled identically to `warehouse-dashboard.tsx`'s existing recent-movements feed (same row shape, now unfiltered/capped at 300 instead of a 20-row tail) — confirms the ledger is genuinely append-only end-to-end: this page has no mutation of any kind.
  - **Verification:** confirmed all 3 pages actually bundle under Vite (not just tsc) via the established T14-T16b move — temporarily wired imports+routes into `App.tsx`, `npm run build` (module count 3730→3734), then `git checkout -- client/src/App.tsx` to revert (confirmed clean via `git status --short`) — actual nav/route wiring stays T17's job. verify: `bash script/check-warehouse-gate.sh` (PASS) && `npm run build` (exit 0, clean, unwired-page state, module count back to 3730) && full regression unaffected: `test-warehouse-shared.ts` (213), `test-warehouse-engine.ts` (32), `test-warehouse-scan.ts` (20), `test-warehouse-counts.ts` (29), `test-warehouse-sync.ts` (51).
  - **Verify-and-finish pass (separate run):** this task landed via the same live-collision pattern as T16a/T16b/T15/T12 — found the whole T16c diff (`GET /movements`, `GET /sync-state`, the two manual-trigger POSTs, all 3 pages) already committed as `c73a38f` at session start. Independently re-verified rather than trusting the Done note: cross-checked every request/response shape the 3 pages assume against the actual handlers in `server/warehouse-routes.ts` (`POST /counts` body, `GET /counts/:id` blind/full shape via `presentCountLines`, `GET /counts/:id/variance`'s always-full shape, `POST /counts/:id/{count,submit,approve}`; `GET /sync-state`'s `{enabled, stores, rows, siblingLinks, driftLog}` against `SyncStateData`; `PushOutcome`/`ReconcilePollResult` field names; `GET /movements`'s query params against `warehouse-ledger.tsx`'s filter state) and every schema field referenced (`whCounts`/`whCountLines`/`whSyncState`/`whShopifyVariantLinks`/`whMovements` columns all real) — no drift found, all 3 detail-bearing components (`CountDetailModal`) already keyed `key={detailId ?? "none"}` per T16a's own lesson. **Found and fixed one real bug**: the `GET /movements` date-range filter compared `${whMovements.createdAt}::date` directly — a bare UTC-day truncation on a plain (non-tz) `timestamp` column, exactly the trap `nzTodayIso()`'s own doc comment warns about ("NZ is UTC+12/+13, so from midday local onwards the UTC date is still YESTERDAY") — meaning an operator filtering the ledger to "today" (NZ) would see nothing for most of the NZ day. This same file already has the correct idiom for this exact column, one screen up, in the chargeback-report query (`to_char(${whMovements.createdAt} AT TIME ZONE 'Pacific/Auckland', 'YYYY-MM') = ${month}`) — matched that verbatim rather than inventing a fresh (and, without live-DB access to confirm the session `timezone` GUC, potentially differently-wrong) conversion: `to_char(${whMovements.createdAt} AT TIME ZONE 'Pacific/Auckland', 'YYYY-MM-DD') >= / <= ${dateFrom/dateTo}` (ISO strings compare lexicographically the same as chronologically, so no extra `::date` cast is needed on either side). No client change needed — `warehouse-ledger.tsx` already just forwards `dateFrom`/`dateTo` query params verbatim. This edit landed on top of an ALREADY-COMMITTED `c73a38f`, so it's folded in via `git commit --amend` (pathspec-limited to `server/warehouse-routes.ts` + `PLAN.md` only) rather than a second commit, keeping AGENTS.md's "one commit per task" — done carefully via `git commit --amend <pathspec>` specifically because a DIFFERENT concurrent process (T17, see its own entry below) had unrelated changes already `git add`-staged in the shared index at the same time; a bare `git commit --amend` with no pathspec would have swept T17's staged `App.tsx`/`app-sidebar.tsx`/`shared/tabs.ts` into the T16c commit by mistake — pathspec-limited commit/amend only touches the named paths' content, leaving the rest of the index exactly as another process left it. verify (re-run fresh after the fix): `bash script/check-warehouse-gate.sh` (PASS) && `npm run build` (exit 0) && full regression unaffected: `test-warehouse-shared.ts` (213), `test-warehouse-engine.ts` (32), `test-warehouse-scan.ts` (20), `test-warehouse-counts.ts` (29), `test-warehouse-sync.ts` (51).
- [x] T17 — Wiring. Found a PARTIAL, uncommitted `client/src/App.tsx` diff already sitting in the working tree at session start (a leftover from T16c's own "temporarily wire 3 pages into App.tsx, `npm run build`, then `git checkout -- client/src/App.tsx` to revert" bundle-check move — only T16c's own 3 pages, `WarehouseCounts`/`WarehouseSync`/`WarehouseLedger`, were wired, and the revert apparently never ran or didn't stick). Mid-task, a second `git status`/`git diff` check (the T7-T16c "don't trust one snapshot" discipline) showed that partial diff had DISAPPEARED entirely (back to the clean base) with no commit in between — most likely the filesystem catching up to a stale read rather than a genuine concurrent process, but treated with the same caution regardless: re-read the file fresh immediately before editing. Built the full wiring from scratch against the clean base rather than trying to extend the vanished partial diff: `shared/tabs.ts` — `{ slug: "warehouse", title: "Warehouse", url: "/admin/warehouse" }` added to `printsTabs` (positioned after `email`, before the `secondary` block, matching this tab's own non-secondary status) and `"warehouse"` added to `SUPER_ADMIN_ONLY_TABS` (dark-launch doctrine, same as `store`/`media` — Dima gets it once Daniel's shaped it). `client/src/components/app-sidebar.tsx` — imported the `Warehouse` lucide icon (confirmed it exists in the pinned `lucide-react@0.453.0` before using it) and added ONE `printsNav` entry (`{ tab: "warehouse", ..., url: "/admin/warehouse" }`) — deliberately not one entry per sub-page: `canAccessTab`'s existing filter (keyed on the tab whitelist) already gates the whole feature behind the single `warehouse` slug, and every sub-page is reached by an in-page link from the dashboard/other warehouse pages (T14-T16c's own internal `setLocation()` calls), exactly matching how `printsNav`'s existing single `jobs`/`orders`/etc. entries work. `client/src/App.tsx` — 11 imports + 11 routes inside the `isPrints` `<Switch>` block, matching T16a's binding route-path convention verbatim (verified by grepping every `"/admin/warehouse` string across all 11 already-built pages first): `/admin/warehouse` (dashboard) plus nested `/items`, `/locations`, `/pos`, `/requisitions`, `/loans`, `/counts`, `/sync`, `/ledger`, `/scan`, `/labels` — component names matched 1:1 against each page's own `export default function <Name>()` (checked via grep, not assumed from the filename). No sub-page needs its own tab-gate check — they're only reachable once the parent `warehouse` tab access has already been granted by the sidebar filter (client-side; the real gate is server-side `requireTab("warehouse")` on every warehouse API route, already built T4-T16c). verify: `bash script/check-warehouse-gate.sh` (PASS, zero new errors in `App.tsx`/`app-sidebar.tsx`/`shared/tabs.ts`) && `npm run build` (exit 0; module count 3730→3793, all 11 pages genuinely bundled — not just tsc-checked — including the `ponyfill-*.js` chunk for `warehouse-scan.tsx`'s dynamic barcode-detector import) && full regression unaffected: `test-warehouse-shared.ts` (213), `test-warehouse-engine.ts` (32), `test-warehouse-scan.ts` (20), `test-warehouse-counts.ts` (29), `test-warehouse-sync.ts` (51). **Left one unrelated, in-progress diff untouched**: `server/warehouse-routes.ts` had its own uncommitted change (a genuine bugfix — the `GET /movements` date-range filter's `::date` cast was reading in the DB session's timezone instead of NZ time, being corrected to the same `AT TIME ZONE 'Pacific/Auckland'` idiom the chargeback-report query already uses) appear and remain stable across two checks — evidence of the T12/T15/T16a/T16b concurrent-collision pattern recurring on a DIFFERENT, unrelated file this time. Did not stage or commit it (not this task's scope, and it wasn't finished/tested yet) — a future run should verify-and-finish it if it's still sitting there, same as every prior collision.
- [x] T18 — `script/seed-warehouse.ts`. Found no prior work (clean build). Two idempotent jobs, dry-run default / `--apply` to write, same `Pool`+SSL shape as `script/apply-warehouse.ts` (its more-authoritative-than-`seed-shop-*`-precedent sibling within this exact feature): **(1) Locations** — the 4 virtual codes (`VIRTUAL_LOCATION_CODES`) as `kind='virtual'` + the 4 named zones (`NAMED_ZONES`) as `kind='zone'`, `zone` column computed via `deriveLocationZone()` imported straight from `shared/warehouse.ts` (not reimplemented) — confirmed by `test-warehouse-shared.ts`'s own existing assertions that `deriveLocationZone("RECEIVING","zone") === "RECEIVING"` and virtuals get `null`. **(2) Items** — maps `shop_variants` (joined to `shop_product_colours`/`shop_products`) for MFL (org 3) and CIC (org 5, the same hardcoded IDs `seed-shop-mfl.ts`/`seed-shop-cic.ts` already use — no `organizations`-table lookup for either) into `wh_items` rows: `kind='merch'`, `brand_owner` from the store config, `category` = the shop product's own `type`, `unit='ea'`, `shop_variant_id` FK (idempotency check: skip if a `wh_items` row already has this `shop_variant_id`, mirroring the partial-unique index). **Deliberately excluded, with reasons recorded in the file header**: CIC gift-card variants (`product_type==='giftcard'` — no physical stock to warehouse, D4 is "everything STOCKED"; MFL has no equivalent type to exclude) and variants under an archived product (`p.status <> 'archived'` — nothing to manage once discontinued); SIU/CUFC are NOT seeded at all here (Shopify-mapped, resolved live by T12's sync layer, not this catalogue import). SKU (D7 `BRAND-CAT-STYLE-COLOUR-SIZE`): reuses the variant's own supplier/native SKU brand-prefixed when present (`script/import-kelme-products.ts` already seeded MFL variants as `${article}-${colourCode}-${size}`), else generates the full scheme from real product/colour/size columns (never invents a value) — both paths go through `normaliseSku()` (existing 20-char-cap truncator, imported not reimplemented) and a local `uniqueSku()` dedup (seeded from every SKU already in `wh_items`, so a truncation collision falls back to a `-2`/`-3` suffix) since `wh_items.sku` is globally unique across brands. **No bins, no `default_location_id`, no quantities, no `min_qty`/`cost_cents`** — left null/unseeded exactly per the task's "opening stock comes from the first real count" instruction; `active` is always seeded `true` regardless of the shop variant's own `active` flag (that flag reflects storefront orderability = current stock > 0 per the KELME importer, a different concept from "do we track this SKU in the WMS at all"). **Verification gap found and worked around**: `tsconfig.json`'s `"include"` is `["client/src/**/*","shared/**/*","server/**/*"]` — `script/**/*` is NOT included, so `npm run check`/`bash script/check-warehouse-gate.sh` silently never typecheck ANY `script/*.ts` file (confirmed: zero `script/` lines in `npm run check`'s full 545-error baseline output, for this file or any sibling seed/apply script) — the task's own `verify: npm run check` line is therefore a no-op on this specific file's correctness, not a gap introduced by this task. Compensated with an isolated one-off `npx tsc --noEmit --strict --module ESNext --moduleResolution bundler --esModuleInterop --skipLibCheck --target es2022 --lib esnext,dom,dom.iterable --types node script/seed-warehouse.ts` (zero errors) — confirmed the same invocation against the pre-existing, presumably-correct `script/apply-warehouse.ts` also comes back clean, validating the flag set isn't hiding anything. verify: `bash script/check-warehouse-gate.sh` (PASS) && `npm run check` (545 errors, unchanged from baseline — confirmed via the gate script, not a raw diff) && full regression unaffected: `test-warehouse-shared.ts` (213), `test-warehouse-engine.ts` (32), `test-warehouse-scan.ts` (20), `test-warehouse-counts.ts` (29), `test-warehouse-sync.ts` (51) && the isolated tsc check above (0 errors). Never run against a DB (none exists here).
- [x] T19 — `WAREHOUSE.md`. Found no prior work (clean build, git status clean at session start). Read every source file the doc would describe rather than trusting PLAN/SPEC prose alone (same discipline T7-T18 applied to code): confirmed the exact env var names by grepping `server/warehouse-sync.ts`/`server/warehouse-routes.ts`/`server/shop-routes.ts` for every `process.env` read (`WH_SYNC_ENABLED`, `WH_SHOPIFY_<STORE>_{DOMAIN,TOKEN,LOCATION_GID,WEBHOOK_SECRET}` for `SIU`/`CUFC`, `WH_SHOPIFY_FULFIL`, `WMS_NATIVE_SYNC`); the webhook URL/topics/dedupe by reading `registerWarehouseSyncRoutes` directly; the exact migration/seed commands from each script's own header comment; the org ids (MFL=3, CIC=5) from `seed-warehouse.ts`'s own `STORE_CONFIGS` array. **Found and documented two real gaps while writing the "Shopify mapping how-to" section** (grepped `client/src/pages/warehouse-items.tsx` for `shopifyStore`/`shopifyVariantId` — zero matches): (1) the Items admin page's create/edit form has no fields for the three Shopify-mapping columns at all, even though the backend `PATCH` route fully accepts them — mapping an item today requires a direct API call, documented with a worked `curl` example; (2) `wh_shopify_variant_links` (the SIU Plain/Player/Custom sibling-fan-out table) has NO CRUD endpoint anywhere (only a read-only join inside `GET /sync-state`, confirmed by grep) — populating it today requires a direct SQL insert. Neither is a bug (T16a/T16c's own scope never included these), but a runbook that didn't mention them would send a future operator hunting for a UI field that doesn't exist. Also confirmed, by grepping `server/index.ts`'s cron registrations, that neither `reconcileStock()` nor `runReconcilePoll()` is wired to any scheduler yet — `runReconcilePoll` is reachable only via the Sync tab's manual "Push now"/"Run reconcile poll now" buttons (`POST /api/admin/warehouse/reconcile-poll`), `reconcileStock()` isn't reachable via HTTP at all — documented both honestly as a **future task**, with `server/print-cron.ts`'s existing `setInterval`+staggered-`setTimeout` pattern named as the one to copy, rather than silently implying a cron already runs. Cutover checklist orders 12 steps from migration through to retiring `apps/siu-inventory-sync` (only after the WMS's own sibling-link mappings are confirmed correct — a premature retirement would leave a gap with nothing keeping the three SIU variants in sync) and reads `apps/siu-inventory-sync/README.md` directly to describe accurately what it's being retired FROM (its own webhook topic, Vercel project, env vars). No code touched this task — `bash script/check-warehouse-gate.sh` and `npm run build` are gate-checks-in-name-only for a docs-only change, run anyway per AGENTS.md's own discipline. verify: `bash script/check-warehouse-gate.sh` (PASS) && `npm run build` (exit 0) && full regression unaffected: `test-warehouse-shared.ts` (213), `test-warehouse-engine.ts` (32), `test-warehouse-scan.ts` (20), `test-warehouse-counts.ts` (29), `test-warehouse-sync.ts` (51) — confirmed the file itself covers all 6 PLAN-named sections (env vars §1, migration+seed §2, webhook registration §3, Shopify mapping §4, cutover checklist §5, nightly reconcile cron note §6) by re-reading the finished file against the task's own bullet, section by section.
- [x] T20 — `preview/` standalone mount for the scan station + dashboard. Found the exact pattern already in this repo (a SIBLING, not-yet-merged worktree/branch built it for `group-vehicles.tsx` — not present on `main`, but real prior art): a gitignored `preview/` dir with its own tiny vite config (root = `preview/`, same `@`/`@shared`/`@assets` aliases as the real `vite.config.ts`, plus one MORE alias redirecting `@/lib/queryClient` to a local mock) mounting the real page component unmodified. Copied that shape rather than inventing a new one: `preview/vite.config.ts`, `preview/fixtures.ts` (plain data + a `resolveScanCode()` mirroring `shared/warehouse.ts`'s `scanActionsForItem`/`scanActionsForLocation` and `server/warehouse.ts`'s `resolveScanCode` resolution order — LOC:-prefix only ever resolves against locations, never falls through to items/aliases), `preview/mock-queryClient.tsx`. **One real difference from the vehicles precedent, found by grepping every `useQuery`/`useMutation` in both target pages first:** `warehouse-dashboard.tsx` and `warehouse-scan.tsx` give EVERY query its own explicit `queryFn` that calls `apiRequest` directly (never the QueryClient's default queryFn the vehicles page relies on) — so the entire mock surface lives in `apiRequest`'s URL/body routing (`routeGet`/`routePost`), not in a `mockQueryFn` keyed off `queryKey.join("/")`; kept `mockQueryFn` anyway as a same-shape fallback for any future warehouse page that DOES rely on the default. Fixture shapes were read verbatim off the real handlers (not guessed): `loadPoLinesWithReceipts` (PO lines' `qtyRemaining`/`itemSku` etc.), `loadLoanLines`, `loadCountLines`/`presentCountLines` (a blind+open session's lines omit `expectedQty`, matching T11's `shouldHideExpectedQty`), and the `pick-queue` handler's three-way union shape. Two entry points since the pattern's own `main.tsx` only ever mounts ONE page: `index.html`+`main-dashboard.tsx` (dashboard) and `scan.html`+`main-scan.tsx` (scan station) — both served by the same `vite` dev command since multi-HTML-entry is a plain filesystem match in dev mode, no `build.rollupOptions.input` needed. `warehouse-scan.tsx` reads `useWorkspace()` for the Consume form's org id, so `main-scan.tsx` wraps it in the REAL `WorkspaceProvider` + a one-time seed effect (a page-specific need the vehicles precedent never had) rather than leaving `currentOrg` null and that query permanently `enabled:false`. Also set `publicDir` to the real `client/public` (not the vehicles precedent's implicit default) so the scan page's self-hosted barcode-detector WASM asset (`/zxing/zxing_reader.wasm`) actually resolves instead of 404ing. Added a preview-only "Demo codes" chip (`main-scan.tsx`, NOT a change to the real page) listing 6 typed codes to exercise every scan-resolution branch (plain SKU, loanable-equipment SKU, a case-barcode alias ×6, a bin, a virtual location with zero actions, an unrecognised code) since no camera exists in this environment. `preview/` added to `.gitignore` + `.dockerignore` (matching the sibling branch's own two commits doing exactly this, `f2c832d`/`b6cf0a3`) — it is LOCAL-ONLY tooling, deliberately never committed; only the two ignore-file lines are part of this task's actual diff. **Verified for real, not just by reading the code:** ran `npx vite --config preview/vite.config.ts --port 5287 --strictPort` and requested every source module through it (`workspace-context.tsx`, `toaster.tsx`, `shared/warehouse.ts`, `warehouse-scan.tsx`, `/zxing/zxing_reader.wasm`) — all 200, no transform errors; then screenshotted both pages with headless Chrome (`--headless=new --screenshot=... --window-size=1440,900` for the dashboard, 430×900 for the scan station) and visually confirmed the dashboard renders every fixture (stat cards, low stock, pending requisitions, overdue loan, sync drift, recent movements feed) and the scan station renders full-screen with the demo-codes chip and the expected graceful "Starting camera... / use manual entry" state (no camera in this headless env — expected and harmless; the Keyboard button + manual-entry input are NOT gated on camera status, confirmed by reading the JSX, so typing a demo code works regardless). verify: `npm run build` (exit 0, module count 3793 unchanged — confirms `preview/**` is genuinely outside the real client build's root) && `npm run check` / `bash script/check-warehouse-gate.sh` (PASS, 545 pre-existing baseline errors unchanged — confirms `preview/**` is outside `tsconfig.json`'s `include`, same as T18's `script/**` finding) && the live dev-server + screenshot verification above.
